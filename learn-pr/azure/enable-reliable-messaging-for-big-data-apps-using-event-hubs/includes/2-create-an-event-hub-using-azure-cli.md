<span data-ttu-id="e83cc-101">Il team ha deciso di sfruttare le funzionalità di Hub eventi di Azure per gestire ed elaborare i crescenti volumi di transazioni gestiti dal sistema.</span><span class="sxs-lookup"><span data-stu-id="e83cc-101">Your team has decided to leverage the capabilities of the Azure Event Hubs to manage and process the increasing transaction volumes coming through your system.</span></span>

<span data-ttu-id="e83cc-102">Un hub eventi è una risorsa di Azure, quindi il primo passaggio consiste nel creare un nuovo hub in Azure e configurarlo per soddisfare gli specifici requisiti delle applicazioni.</span><span class="sxs-lookup"><span data-stu-id="e83cc-102">An Event Hub is an Azure resource, so your first step is to create a new hub in Azure and configure it to meet the specific requirements of your applications.</span></span>

## <a name="what-is-an-azure-event-hub"></a><span data-ttu-id="e83cc-103">Che cos'è un hub eventi di Azure?</span><span class="sxs-lookup"><span data-stu-id="e83cc-103">What is an Azure Event Hub?</span></span>

<span data-ttu-id="e83cc-104">Hub eventi di Azure è un servizio di elaborazione di eventi basato sul cloud, in grado di ricevere ed elaborare milioni di eventi al secondo.</span><span class="sxs-lookup"><span data-stu-id="e83cc-104">Azure Event Hubs is a cloud-based, event-processing service that's capable of receiving and processing millions of events per second.</span></span> <span data-ttu-id="e83cc-105">Hub eventi opera come una porta principale per una pipeline di eventi, poiché riceve i dati in ingresso e li archivia finché non sono disponibili le risorse di elaborazione.</span><span class="sxs-lookup"><span data-stu-id="e83cc-105">Event Hubs acts as a front door for an event pipeline, where it receives incoming data and stores it until processing resources are available.</span></span>

<span data-ttu-id="e83cc-106">Un'entità che invia dati a Hub eventi è denominata *autore*, mentre un'entità che legge i dati da Hub eventi è denominata *consumer* o *sottoscrittore*.</span><span class="sxs-lookup"><span data-stu-id="e83cc-106">An entity that sends data to the Event Hubs is called a *publisher* and an entity that reads data from the Event Hubs is called a *consumer* or a *subscriber*.</span></span> <span data-ttu-id="e83cc-107">Hub eventi di Azure è posizionato tra queste due entità per suddividere la produzione (dall'autore) e l'uso (verso il sottoscrittore) di un flusso di eventi.</span><span class="sxs-lookup"><span data-stu-id="e83cc-107">Azure Event Hubs sits between these two entities to divide the production (from publisher) and consumption (to subscriber) of an event stream.</span></span> <span data-ttu-id="e83cc-108">Questa separazione consente di gestire scenari in cui il tasso di produzione di eventi è notevolmente superiore al consumo.</span><span class="sxs-lookup"><span data-stu-id="e83cc-108">This decoupling helps to manage scenarios where the rate of event production is much higher than the consumption.</span></span> <span data-ttu-id="e83cc-109">La figura seguente mostra il ruolo di un hub eventi.</span><span class="sxs-lookup"><span data-stu-id="e83cc-109">The following illustration shows the role of an Event Hub.</span></span>

![Figura che mostra un hub eventi di Azure posizionato tra quattro autori e due sottoscrittori.](../media/2-event-hub-overview.png)

### <a name="events"></a><span data-ttu-id="e83cc-112">Eventi</span><span class="sxs-lookup"><span data-stu-id="e83cc-112">Events</span></span>

<span data-ttu-id="e83cc-113">Un **evento** è un pacchetto di informazioni di piccole dimensioni (un *datagramma*) che contiene una notifica.</span><span class="sxs-lookup"><span data-stu-id="e83cc-113">An **event** is a small packet of information (a *datagram*) that contains a notification.</span></span> <span data-ttu-id="e83cc-114">Gli eventi possono essere pubblicati singolarmente o in batch, ma una pubblicazione (singola o in batch) non può superare i 256 KB.</span><span class="sxs-lookup"><span data-stu-id="e83cc-114">Events can be published individually, or in batches, but a single publication (individual or batch) can't exceed 256 KB.</span></span>

### <a name="publishers-and-subscribers"></a><span data-ttu-id="e83cc-115">Autori e sottoscrittori</span><span class="sxs-lookup"><span data-stu-id="e83cc-115">Publishers and subscribers</span></span>

<span data-ttu-id="e83cc-116">Un autore di eventi è qualsiasi applicazione o dispositivo in grado di inviare eventi tramite HTTPS o Advance Message Queueing Protocol (AMQP) 1.0.</span><span class="sxs-lookup"><span data-stu-id="e83cc-116">Event publishers are any application or device that can send out events using either HTTPS or Advanced Message Queuing Protocol (AMQP) 1.0.</span></span>

<span data-ttu-id="e83cc-117">Per gli autori che inviano dati di frequente, AMQP offre le migliori prestazioni.</span><span class="sxs-lookup"><span data-stu-id="e83cc-117">For publishers that send data frequently, AMQP has the better performance.</span></span> <span data-ttu-id="e83cc-118">Tuttavia, presenta un maggiore sovraccarico iniziale per le sessioni, perché è prima necessario configurare un socket bidirezionale persistente e TLS (Transport Layer Security) o SSL/TLS.</span><span class="sxs-lookup"><span data-stu-id="e83cc-118">However, it has a higher initial session overhead, because a persistent bidirectional socket and transport level security (TLS) or SSL/TLS has to be set up first.</span></span> 

<span data-ttu-id="e83cc-119">Per una pubblicazione più intermittente, è preferibile HTTPS.</span><span class="sxs-lookup"><span data-stu-id="e83cc-119">For more intermittent publishing, HTTPS is the better option.</span></span> <span data-ttu-id="e83cc-120">Anche se HTTPS richiede un sovraccarico aggiuntivo per ogni richiesta, non presenta il sovraccarico di inizializzazione della sessione.</span><span class="sxs-lookup"><span data-stu-id="e83cc-120">Though HTTPS requires additional overhead for each request, there isn’t the session initialization overhead.</span></span>

> [!NOTE] 
> <span data-ttu-id="e83cc-121">Anche i client esistenti basati su Kafka, che usano client Apache Kafka 1.0 e versioni successive, possono operare come autori per Hub eventi.</span><span class="sxs-lookup"><span data-stu-id="e83cc-121">Existing Kafka-based clients, using Apache Kafka 1.0 and newer client versions, can also act as Event Hubs publishers.</span></span>

<span data-ttu-id="e83cc-122">I sottoscrittori di eventi sono applicazioni che usano uno dei due metodi supportati a livello di programmazione per ricevere ed elaborare gli eventi da un hub eventi.</span><span class="sxs-lookup"><span data-stu-id="e83cc-122">Event subscribers are applications that use one of two supported programmatic methods to receive and process events from an Event Hub.</span></span>

- <span data-ttu-id="e83cc-123">**EventHubReceiver**: un metodo semplice che include opzioni di gestione limitate.</span><span class="sxs-lookup"><span data-stu-id="e83cc-123">**EventHubReceiver** - A simple method that provides limited management options.</span></span>
- <span data-ttu-id="e83cc-124">**EventProcessorHost**: un metodo efficiente che verrà usato più avanti in questo modulo.</span><span class="sxs-lookup"><span data-stu-id="e83cc-124">**EventProcessorHost** - An efficient method that we’ll use later in this module.</span></span>

### <a name="consumer-groups"></a><span data-ttu-id="e83cc-125">Gruppi di consumer</span><span class="sxs-lookup"><span data-stu-id="e83cc-125">Consumer groups</span></span>

<span data-ttu-id="e83cc-126">Un **gruppo di consumer** di un hub eventi rappresenta una visualizzazione specifica di un flusso di dati dell'hub eventi.</span><span class="sxs-lookup"><span data-stu-id="e83cc-126">An Event Hub **consumer group** represents a specific view of an Event Hub data stream.</span></span> <span data-ttu-id="e83cc-127">Usando gruppi di consumer distinti, più applicazioni sottoscrittore possono elaborare un flusso di eventi in modo indipendente e senza influire sulle altre applicazioni.</span><span class="sxs-lookup"><span data-stu-id="e83cc-127">By using separate consumer groups, multiple subscriber applications can process an event stream independently, and without affecting other applications.</span></span> <span data-ttu-id="e83cc-128">Tuttavia, l'uso di più gruppi di consumer non è un requisito e per molte applicazioni il gruppo di consumer predefinito è sufficiente.</span><span class="sxs-lookup"><span data-stu-id="e83cc-128">However, the use of multiple consumer groups is not a requirement, and for many applications, the single default consumer group is sufficient.</span></span>

### <a name="pricing"></a><span data-ttu-id="e83cc-129">Prezzi</span><span class="sxs-lookup"><span data-stu-id="e83cc-129">Pricing</span></span>

<span data-ttu-id="e83cc-130">Sono disponibili tre piani tariffari per Hub eventi di Azure: Basic, Standard e Dedicato.</span><span class="sxs-lookup"><span data-stu-id="e83cc-130">There are three pricing tiers for Azure Event Hubs: Basic, Standard, and Dedicated.</span></span> <span data-ttu-id="e83cc-131">I livelli presentano differenze in termini di connessioni supportate, numero di gruppi di consumer disponibili e velocità effettiva.</span><span class="sxs-lookup"><span data-stu-id="e83cc-131">The tiers differ in terms of supported connections, number of available Consumer groups, and throughput.</span></span> <span data-ttu-id="e83cc-132">Quando si usa l'interfaccia della riga di comando di Azure per creare uno spazio dei nomi di Hub eventi, se non si specifica un piano tariffario, viene assegnato il valore predefinito **Standard** (20 gruppi di consumer, 1000 connessioni negoziate).</span><span class="sxs-lookup"><span data-stu-id="e83cc-132">When using Azure CLI to create an Event Hubs namespace, if you don't specify a pricing tier, the default of **Standard** (20 Consumer groups, 1000 Brokered connections) is assigned.</span></span>

## <a name="creating-and-configuring-a-new-azure-event-hubs"></a><span data-ttu-id="e83cc-133">Creazione e configurazione di un nuovo hub eventi di Azure</span><span class="sxs-lookup"><span data-stu-id="e83cc-133">Creating and configuring a new Azure Event Hubs</span></span>

<span data-ttu-id="e83cc-134">La creazione e la configurazione di un nuovo hub eventi di Azure comprende due passaggi principali.</span><span class="sxs-lookup"><span data-stu-id="e83cc-134">There are two main steps when creating and configuring a new Azure Event Hubs.</span></span> <span data-ttu-id="e83cc-135">Il primo passaggio consiste nel definire lo **spazio dei nomi** di Hub eventi.</span><span class="sxs-lookup"><span data-stu-id="e83cc-135">The first step is to define an Event Hubs **namespace**.</span></span> <span data-ttu-id="e83cc-136">Il secondo passaggio consiste nel creare un hub eventi nello spazio dei nomi.</span><span class="sxs-lookup"><span data-stu-id="e83cc-136">The second step is to create an Event Hub in that namespace.</span></span>

### <a name="defining-an-event-hubs-namespace"></a><span data-ttu-id="e83cc-137">Definizione di uno spazio dei nomi di Hub eventi</span><span class="sxs-lookup"><span data-stu-id="e83cc-137">Defining an Event Hubs namespace</span></span>

<span data-ttu-id="e83cc-138">Uno spazio dei nomi di Hub eventi è un'entità contenitore per la gestione di uno o più hub eventi.</span><span class="sxs-lookup"><span data-stu-id="e83cc-138">An Event Hubs namespace is a containing entity for managing one or more Event Hubs.</span></span> <span data-ttu-id="e83cc-139">La creazione di uno spazio dei nomi di Hub eventi in genere comporta quanto segue:</span><span class="sxs-lookup"><span data-stu-id="e83cc-139">Creating an Event Hubs namespace typically involves the following:</span></span>

1. <span data-ttu-id="e83cc-140">Definizione delle impostazioni a livello di spazio dei nomi.</span><span class="sxs-lookup"><span data-stu-id="e83cc-140">Defining namespace-level settings.</span></span> <span data-ttu-id="e83cc-141">Alcune impostazioni, come la capacità dello spazio dei nomi (configurata tramite le **unità elaborate**), il piano tariffario e le metriche delle prestazioni, sono definite a livello di spazio dei nomi.</span><span class="sxs-lookup"><span data-stu-id="e83cc-141">Certain settings such as namespace capacity (configured using **throughput units**), pricing tier, and performance metrics are defined at the namespace level.</span></span> <span data-ttu-id="e83cc-142">Queste sono applicabili per tutti gli hub eventi nello spazio dei nomi.</span><span class="sxs-lookup"><span data-stu-id="e83cc-142">These are applicable for all the Event Hubs within that namespace.</span></span> <span data-ttu-id="e83cc-143">Se non si definiscono queste impostazioni, viene usato un valore predefinito: *1* per la capacità e *Standard* per il piano tariffario.</span><span class="sxs-lookup"><span data-stu-id="e83cc-143">If you don't define these settings, a default value is used: *1* for capacity and *Standard* for pricing tier.</span></span>

    <span data-ttu-id="e83cc-144">Una volta impostate le unità elaborate, non è possibile modificarle.</span><span class="sxs-lookup"><span data-stu-id="e83cc-144">You cannot change the throughput unit once you set it.</span></span> <span data-ttu-id="e83cc-145">È necessario bilanciare la configurazione rispetto alle aspettative per il budget di Azure.</span><span class="sxs-lookup"><span data-stu-id="e83cc-145">You must balance your configuration against your Azure budget expectations.</span></span> <span data-ttu-id="e83cc-146">È possibile valutare la configurazione di diversi hub eventi per differenti requisiti di velocità effettiva.</span><span class="sxs-lookup"><span data-stu-id="e83cc-146">You might consider configuring different Event Hubs for different throughput requirements.</span></span> <span data-ttu-id="e83cc-147">Ad esempio, se si dispone di un'applicazione per i dati di vendita e si prevede di usare due hub eventi (uno per la raccolta con una velocità effettiva elevata di informazioni di telemetria in tempo reale sui dati di vendita e uno per la raccolta dei log eventi con una minore frequenza), potrebbe essere utile usare uno spazio dei nomi distinto per ogni hub.</span><span class="sxs-lookup"><span data-stu-id="e83cc-147">For example, if you have a sales data application and you are planning for two Event Hubs, one for high throughput collection of real-time sales data telemetry and one for infrequent event log collection, it would make sense to use a separate namespace for each hub.</span></span> <span data-ttu-id="e83cc-148">In questo modo, è sufficiente configurare (ed effettuare il pagamento per) la capacità con velocità effettiva elevata nell'hub di telemetria.</span><span class="sxs-lookup"><span data-stu-id="e83cc-148">This way you only need to configure (and pay for) high throughput capacity on the telemetry hub.</span></span>

1. <span data-ttu-id="e83cc-149">Selezione di un nome univoco per lo spazio dei nomi.</span><span class="sxs-lookup"><span data-stu-id="e83cc-149">Selecting a unique name for the namespace.</span></span> <span data-ttu-id="e83cc-150">Lo spazio dei nomi è accessibile all'URL: *_spazio dei nomi_.servicebus.windows.net*</span><span class="sxs-lookup"><span data-stu-id="e83cc-150">The namespace is accessible through this url: *_namespace_.servicebus.windows.net*</span></span>

1. <span data-ttu-id="e83cc-151">Definizione delle proprietà facoltative seguenti:</span><span class="sxs-lookup"><span data-stu-id="e83cc-151">Defining the following optional properties:</span></span>

    - <span data-ttu-id="e83cc-152">Abilita Kafka.</span><span class="sxs-lookup"><span data-stu-id="e83cc-152">Enable Kafka.</span></span> <span data-ttu-id="e83cc-153">Questa opzione consente alle applicazioni Kafka di pubblicare eventi nell'hub eventi.</span><span class="sxs-lookup"><span data-stu-id="e83cc-153">This option enables Kafka applications to publish events to the Event Hub.</span></span>
    - <span data-ttu-id="e83cc-154">Imposta la ridondanza della zona per questo spazio dei nomi.</span><span class="sxs-lookup"><span data-stu-id="e83cc-154">Make this namespace zone redundant.</span></span> <span data-ttu-id="e83cc-155">La ridondanza della zona replica i dati tra data center distinti con infrastrutture di alimentazione, rete e raffreddamento indipendenti.</span><span class="sxs-lookup"><span data-stu-id="e83cc-155">Zone-redundancy replicates data across separate data centers with their own independent power, networking, and cooling infrastructures.</span></span>
    - <span data-ttu-id="e83cc-156">Abilita aumento automatico e Numero massimo di unità elaborate per l'aumento automatico.</span><span class="sxs-lookup"><span data-stu-id="e83cc-156">Enable Auto-Inflate, and Auto-Inflate Maximum Throughput Units.</span></span> <span data-ttu-id="e83cc-157">L'aumento automatico offre un'opzione per la scalabilità, aumentando il numero di unità elaborate fino a un valore massimo.</span><span class="sxs-lookup"><span data-stu-id="e83cc-157">Auto-Inflate provides an automatic scale-up option, by increasing the number of throughput units up to a maximum value.</span></span> <span data-ttu-id="e83cc-158">Questo è utile per evitare la limitazione delle richieste nelle situazioni in cui le velocità dei dati in ingresso o in uscita superano il numero di unità elaborate attualmente impostato.</span><span class="sxs-lookup"><span data-stu-id="e83cc-158">This is useful to avoid throttling in situations when incoming or outgoing data rates exceed the currently set number of throughput units.</span></span>

### <a name="azure-cli-commands-for-creating-an-event-hubs-namespace"></a><span data-ttu-id="e83cc-159">Comandi dell'interfaccia della riga di comando di Azure per la creazione di uno spazio dei nomi di Hub eventi</span><span class="sxs-lookup"><span data-stu-id="e83cc-159">Azure CLI commands for creating an Event Hubs namespace</span></span>

<span data-ttu-id="e83cc-160">Per creare un nuovo spazio dei nomi di Hub eventi, si useranno i comandi `az eventhubs namespace`.</span><span class="sxs-lookup"><span data-stu-id="e83cc-160">To create a new Event Hubs namespace, you will use the `az eventhubs namespace` commands.</span></span> <span data-ttu-id="e83cc-161">Ecco una breve descrizione dei comandi secondari che verranno usati nell'esercizio.</span><span class="sxs-lookup"><span data-stu-id="e83cc-161">Here's a brief description of the sub-commands we will use in the exercise.</span></span>

| <span data-ttu-id="e83cc-162">Comando</span><span class="sxs-lookup"><span data-stu-id="e83cc-162">Command</span></span> | <span data-ttu-id="e83cc-163">Descrizione</span><span class="sxs-lookup"><span data-stu-id="e83cc-163">Description</span></span> |
|---------|-------------|
| `create` | <span data-ttu-id="e83cc-164">Creare lo spazio dei nomi di Hub eventi.</span><span class="sxs-lookup"><span data-stu-id="e83cc-164">Create the Event Hubs namespace.</span></span> |
| `authorization-rule` | <span data-ttu-id="e83cc-165">Tutti gli hub eventi nello stesso spazio dei nomi di Hub eventi condividono credenziali di connessione comuni.</span><span class="sxs-lookup"><span data-stu-id="e83cc-165">All Event Hubs within the same Event Hubs namespace share common connection credentials.</span></span> <span data-ttu-id="e83cc-166">Queste credenziali saranno necessarie per configurare le applicazioni per inviare e ricevere messaggi tramite l'hub eventi.</span><span class="sxs-lookup"><span data-stu-id="e83cc-166">You will need these credentials when you configure applications to send and receive messages using the Event Hub.</span></span> <span data-ttu-id="e83cc-167">Questo comando restituisce la stringa di connessione per lo spazio dei nomi di Hub eventi.</span><span class="sxs-lookup"><span data-stu-id="e83cc-167">This command returns the connection string for your Event Hubs namespace.</span></span> |

### <a name="configuring-a-new-event-hub"></a><span data-ttu-id="e83cc-168">Configurazione di un nuovo hub eventi</span><span class="sxs-lookup"><span data-stu-id="e83cc-168">Configuring a new Event Hub</span></span>

<span data-ttu-id="e83cc-169">Dopo aver creato lo spazio dei nomi di Hub eventi, è possibile creare un hub eventi.</span><span class="sxs-lookup"><span data-stu-id="e83cc-169">After the Event Hubs namespace has been created, you can create an Event Hub.</span></span> <span data-ttu-id="e83cc-170">Quando si crea un nuovo hub eventi, esistono alcuni parametri obbligatori.</span><span class="sxs-lookup"><span data-stu-id="e83cc-170">When creating a new Event Hub, there are several mandatory parameters.</span></span>

<span data-ttu-id="e83cc-171">Per creare un hub eventi, sono necessari i parametri seguenti:</span><span class="sxs-lookup"><span data-stu-id="e83cc-171">The following parameters are required to create an Event Hub:</span></span>

- <span data-ttu-id="e83cc-172">**Nome hub eventi**: nome dell'hub eventi. Deve essere univoco all'interno della sottoscrizione e:</span><span class="sxs-lookup"><span data-stu-id="e83cc-172">**Event Hub name** - Event Hub name that is unique within your subscription and:</span></span>
  - <span data-ttu-id="e83cc-173">Deve avere una lunghezza compresa tra 1 e 50 caratteri.</span><span class="sxs-lookup"><span data-stu-id="e83cc-173">Is between 1 and 50 characters long</span></span>
  - <span data-ttu-id="e83cc-174">Deve contenere solo lettere, numeri, punti, trattini e caratteri di sottolineatura.</span><span class="sxs-lookup"><span data-stu-id="e83cc-174">Contains only letters, numbers, periods, hyphens, and underscores</span></span>
  - <span data-ttu-id="e83cc-175">Deve iniziare e terminare con una lettera o un numero.</span><span class="sxs-lookup"><span data-stu-id="e83cc-175">Starts and ends with a letter or number</span></span>
- <span data-ttu-id="e83cc-176">**Numero di partizioni**: numero di partizioni necessarie in un hub eventi (compreso tra 2 e 32).</span><span class="sxs-lookup"><span data-stu-id="e83cc-176">**Partition Count** -  The number of partitions required in an Event Hub (between 2 and 32).</span></span> <span data-ttu-id="e83cc-177">Deve essere direttamente correlato al numero di consumer concorrenti previsto.</span><span class="sxs-lookup"><span data-stu-id="e83cc-177">This should be directly related to the expected number of concurrent consumers.</span></span> <span data-ttu-id="e83cc-178">Questa opzione non può essere modificata dopo la creazione dell'hub.</span><span class="sxs-lookup"><span data-stu-id="e83cc-178">This cannot be changed after the hub has been created.</span></span> <span data-ttu-id="e83cc-179">La partizione separa il flusso dei messaggi, in modo che per le applicazioni consumer o ricevitore sia sufficiente leggere uno specifico subset del flusso di dati.</span><span class="sxs-lookup"><span data-stu-id="e83cc-179">The partition separates the message stream, so that consumer or receiver applications only need to read a specific subset of the data stream.</span></span> <span data-ttu-id="e83cc-180">Se non è definito, il valore predefinito è *4*.</span><span class="sxs-lookup"><span data-stu-id="e83cc-180">If not defined, this defaults to *4*.</span></span>
- <span data-ttu-id="e83cc-181">**Conservazione messaggi**: numero di giorni (compreso tra 1 e 7) per cui rimarranno disponibili i messaggi, se dovesse essere necessario riprodurre il flusso dei dati per qualsiasi motivo.</span><span class="sxs-lookup"><span data-stu-id="e83cc-181">**Message Retention** - The number of days (between 1 and 7) that messages will remain available, if the data stream needs to be replayed for any reason.</span></span> <span data-ttu-id="e83cc-182">Se non è definito, il valore predefinito è *7*.</span><span class="sxs-lookup"><span data-stu-id="e83cc-182">If not defined, this defaults to *7*.</span></span>

<span data-ttu-id="e83cc-183">È anche possibile configurare un hub eventi per trasmettere i dati a un account di Archiviazione BLOB di Azure o Azure Data Lake Store.</span><span class="sxs-lookup"><span data-stu-id="e83cc-183">You can also optionally configure an Event Hub to stream data to an Azure Blob storage or Azure Data Lake Store account.</span></span>

### <a name="azure-cli-commands-for-creating-an-event-hub"></a><span data-ttu-id="e83cc-184">Comandi dell'interfaccia della riga di comando di Azure per la creazione di un hub eventi</span><span class="sxs-lookup"><span data-stu-id="e83cc-184">Azure CLI commands for creating an Event Hub</span></span>

<span data-ttu-id="e83cc-185">Per creare un nuovo hub eventi con l'interfaccia della riga di comando di Azure, si userà il set di comandi `az eventhubs eventhub`.</span><span class="sxs-lookup"><span data-stu-id="e83cc-185">To create a new Event Hub with the Azure CLI, you will use the `az eventhubs eventhub` command set.</span></span> <span data-ttu-id="e83cc-186">Ecco una breve descrizione dei comandi secondari che verranno usati:</span><span class="sxs-lookup"><span data-stu-id="e83cc-186">Here's a brief description of the sub-commands we will be using:</span></span>

| <span data-ttu-id="e83cc-187">Comando</span><span class="sxs-lookup"><span data-stu-id="e83cc-187">Command</span></span> | <span data-ttu-id="e83cc-188">Descrizione</span><span class="sxs-lookup"><span data-stu-id="e83cc-188">Description</span></span> |
|---------|-------------|
| `create` | <span data-ttu-id="e83cc-189">Crea l'hub eventi in uno spazio dei nomi specificato.</span><span class="sxs-lookup"><span data-stu-id="e83cc-189">This creates the Event Hub in a specified namespace.</span></span> |
| `show` | <span data-ttu-id="e83cc-190">Consente di visualizzare i dettagli dell'hub eventi.</span><span class="sxs-lookup"><span data-stu-id="e83cc-190">This displays the details of your Event Hub.</span></span> |

## <a name="summary"></a><span data-ttu-id="e83cc-191">Riepilogo</span><span class="sxs-lookup"><span data-stu-id="e83cc-191">Summary</span></span>

<span data-ttu-id="e83cc-192">Per distribuire Hub eventi di Azure, è necessario configurare uno spazio dei nomi di Hub eventi e quindi configurare l'hub eventi stesso.</span><span class="sxs-lookup"><span data-stu-id="e83cc-192">To deploy Azure Event Hubs, you must configure an Event Hubs namespace and then configure the Event Hub itself.</span></span> <span data-ttu-id="e83cc-193">Nella sezione successiva verrà esaminata la procedura di configurazione dettagliata per creare un nuovo spazio dei nomi e un hub eventi.</span><span class="sxs-lookup"><span data-stu-id="e83cc-193">In the next section, you will go through the detailed configuration steps to create a new namespace and Event Hub.</span></span>