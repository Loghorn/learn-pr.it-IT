<span data-ttu-id="6d2fa-101">Tenere presente che l'obiettivo è spostare un server Linux esistente che esegue Apache in Azure.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-101">Recall that our goal is to move an existing Linux server running Apache to Azure.</span></span> <span data-ttu-id="6d2fa-102">Per iniziare verrà creato un server Ubuntu Linux.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-102">We'll start by creating an Ubuntu Linux server.</span></span>

## <a name="create-a-new-linux-virtual-machine"></a><span data-ttu-id="6d2fa-103">Creare una nuova macchina virtuale Linux</span><span class="sxs-lookup"><span data-stu-id="6d2fa-103">Create a new Linux virtual machine</span></span>

<span data-ttu-id="6d2fa-104">È possibile creare macchine virtuali Linux con il portale di Azure, l'interfaccia della riga di comando di Azure o Azure PowerShell.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-104">We can create Linux VMs with the Azure portal, the Azure CLI, or Azure PowerShell.</span></span> <span data-ttu-id="6d2fa-105">L'approccio più semplice quando si inizia a usare Azure prevede l'uso del portale, perché richiede le informazioni necessarie in sequenza e fornisce suggerimenti e messaggi utili durante la creazione:</span><span class="sxs-lookup"><span data-stu-id="6d2fa-105">The easiest approach when you are starting with Azure is to use the portal because it walks you through the required information and provides hints and helpful messages during the creation:</span></span>

1. <span data-ttu-id="6d2fa-106">Accedere al [portale di Azure](https://portal.azure.com/learn.docs.microsoft.com?azure-portal=true) usando lo stesso account con cui è stato attivato l'ambiente sandbox.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-106">Sign into the [Azure portal](https://portal.azure.com/learn.docs.microsoft.com?azure-portal=true) using the same account you activated the sandbox with.</span></span>

1. <span data-ttu-id="6d2fa-107">Fare clic su **Crea una risorsa** nell'angolo in alto a sinistra del portale di Azure.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-107">Click **Create a resource** in the upper-left corner of the Azure portal.</span></span>

1. <span data-ttu-id="6d2fa-108">Nella casella di ricerca immettere **Ubuntu Server** per visualizzare le diverse versioni disponibili.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-108">In the search box, enter  **Ubuntu Server** to see the different versions available.</span></span> <span data-ttu-id="6d2fa-109">Selezionare **Ubuntu Server 18.04 LTS** nell'elenco presentato.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-109">Select **Ubuntu Server 18.04 LTS** from the presented list.</span></span>

1. <span data-ttu-id="6d2fa-110">Fare clic sul pulsante **Crea** per avviare la configurazione della macchina virtuale.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-110">Click the **Create** button to start configuring the VM.</span></span>

## <a name="configure-the-vm-settings"></a><span data-ttu-id="6d2fa-111">Configurare le impostazioni della macchina virtuale</span><span class="sxs-lookup"><span data-stu-id="6d2fa-111">Configure the VM settings</span></span>

<span data-ttu-id="6d2fa-112">L'esperienza di creazione della macchina virtuale nel portale viene presentata sotto forma di procedura guidata, che offre tutte le informazioni dettagliate necessarie per le aree di configurazione della macchina virtuale.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-112">The VM creation experience in the portal is presented in a wizard format to walk you through all the configuration areas for the VM.</span></span> <span data-ttu-id="6d2fa-113">Facendo clic sul pulsante **Avanti** si passerà alla sezione configurabile successiva.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-113">Clicking the **Next** button will take you to the next configurable section.</span></span> <span data-ttu-id="6d2fa-114">È comunque possibile spostarsi tra le sezioni nel modo preferito usando le schede nella parte superiore che identificano ogni parte.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-114">However, you can move between the sections at will with the tabs running across the top that identify each part.</span></span>

![Screenshot del portale di Azure con il pannello Crea macchina virtuale iniziale per un computer server Ubuntu.](../media/3-azure-portal-create-vm.png)

<span data-ttu-id="6d2fa-116">Dopo aver compilato tutte le opzioni obbligatorie identificate con asterischi rossi, è possibile ignorare il resto della procedura guidata e iniziare a creare la macchina virtuale tramite il pulsante **Rivedi e crea** nella parte inferiore.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-116">Once you fill in all the required options (identified with red asterisks), you can skip the remainder of the wizard experience and start creating the VM through the **Review + Create** button at the bottom.</span></span>

<span data-ttu-id="6d2fa-117">Si inizierà con la sezione **Informazioni di base**.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-117">We'll start with the **Basics** section.</span></span>

### <a name="configure-basic-vm-settings"></a><span data-ttu-id="6d2fa-118">Configurare le impostazioni di base della macchina virtuale</span><span class="sxs-lookup"><span data-stu-id="6d2fa-118">Configure basic VM settings</span></span>

1. <span data-ttu-id="6d2fa-119">Per **sottoscrizione** dovrebbe essere selezionata per impostazione predefinita la sottoscrizione del sandbox.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-119">For **Subscription**, the sandbox subscription should be selected for you by default.</span></span>

1. <span data-ttu-id="6d2fa-120">Per **Gruppo di risorse** dovrebbe essere selezionato per impostazione predefinita il gruppo **<rgn>[nome gruppo di risorse sandbox]</rgn>**.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-120">For **Resource group**, the resource group with the name **<rgn>[sandbox resource group name]</rgn>** should be selected for you by default.</span></span>

1. <span data-ttu-id="6d2fa-121">Nella sezione **DETTAGLI ISTANZA** immettere un nome per la macchina virtuale del server Web, ad esempio **test-web-eus-vm1**.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-121">In the **INSTANCE DETAILS** section, enter a name for your web server VM, such as **test-web-eus-vm1**.</span></span> <span data-ttu-id="6d2fa-122">Questo nome indica l'ambiente (**test**), il ruolo (**web**), la località (**eus** ossia Stati Uniti orientali), il servizio (**vm**) e il numero di istanza (**1**).</span><span class="sxs-lookup"><span data-stu-id="6d2fa-122">This indicates the environment (**test**), the role (**web**), location (**East US**), service (**vm**), and instance number (**1**).</span></span>
    - <span data-ttu-id="6d2fa-123">È consigliabile standardizzare i nomi delle risorse per poterne identificare rapidamente lo scopo.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-123">It's a best practice to standardize your resource names, so you can quickly identify their purpose.</span></span> <span data-ttu-id="6d2fa-124">I nomi di macchine virtuali Linux devono avere una lunghezza compresa tra 1 e 64 caratteri ed essere composti da numeri, lettere e trattini.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-124">Linux VM names must be between 1 and 64 characters and be comprised of numbers, letters, and dashes.</span></span>

    > [!NOTE]
    > <span data-ttu-id="6d2fa-125">Quando si modificano le impostazioni e si esce da ogni campo di testo libero, Azure convalida automaticamente ogni valore e visualizza un segno di spunta verde accanto se è corretto.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-125">As you change settings and tab out of each free-text field, Azure will validate each value automatically and place a green check mark next to it when it's good.</span></span> <span data-ttu-id="6d2fa-126">È possibile passare il mouse sopra gli indicatori di errore per ottenere altre informazioni sui problemi individuati.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-126">You can hover over error indicators to get more information on issues it discovers.</span></span>

1. <span data-ttu-id="6d2fa-127">Selezionare una località.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-127">Select a location.</span></span>

    <span data-ttu-id="6d2fa-128"><!-- Resource selection --> [!include[](../../../includes/azure-sandbox-regions-first-mention-note-friendly.md)]</span><span class="sxs-lookup"><span data-stu-id="6d2fa-128"><!-- Resource selection --> [!include[](../../../includes/azure-sandbox-regions-first-mention-note-friendly.md)]</span></span>

1. <span data-ttu-id="6d2fa-129">Per **Availability options** (Opzioni di disponibilità) lasciare selezionato **No infrastructure redundancy required** (Ridondanza infrastruttura non necessaria).</span><span class="sxs-lookup"><span data-stu-id="6d2fa-129">Leave **Availability options** as **No infrastructure redundancy required**.</span></span> <span data-ttu-id="6d2fa-130">Questa opzione viene usata per assicurarsi che la macchina virtuale sia a disponibilità elevata mediante il raggruppamento di più macchine virtuali come set per gestire gli eventi di manutenzione pianificata o non pianificata o le interruzioni.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-130">This option is used to ensure the VM is highly available by grouping multiple VMs together as a set to deal with planned or unplanned maintenance events or outages.</span></span>

1. <span data-ttu-id="6d2fa-131">Verificare che l'immagine sia impostata su **Ubuntu Server 18.04 LTS**.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-131">Ensure that the image is set to **Ubuntu Server 18.04 LTS**.</span></span> <span data-ttu-id="6d2fa-132">È possibile aprire l'elenco a discesa per vedere tutte le opzioni disponibili.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-132">You can open the drop-down list to see all the options available.</span></span>

1. <span data-ttu-id="6d2fa-133">Il campo **Dimensioni** non è direttamente modificabile ed è impostato sulla dimensione predefinita **DS2_v3**, che è una delle selezioni di calcolo per utilizzo generico.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-133">The **Size** field is not directly editable and has a **DS2_v3** default size, which is one of the general-purpose computing selections.</span></span> <span data-ttu-id="6d2fa-134">Questa scelta è perfetta per un server Web pubblico, ma fare clic sul collegamento **Modifica dimensioni** per esplorare altre dimensioni di macchina virtuale.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-134">This choice is perfect for a public web server, but click the **Change size** link to explore other VM sizes.</span></span> <span data-ttu-id="6d2fa-135">Nella finestra di dialogo risultante è possibile filtrare in base al **numero di vCPU**, al **nome** e al **tipo di disco**.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-135">The resulting dialog allows you to filter based on **# of vCPUs**, **Name**, and **Disk Type**.</span></span> <span data-ttu-id="6d2fa-136">Selezionare di nuovo **DS2_v3** che include due vCPU con 8 GB di RAM.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-136">Select the same **DS2_v3** choice, which gives you two vCPUs with 8 GB of RAM.</span></span>

    > [!TIP]
    > <span data-ttu-id="6d2fa-137">È anche possibile far scorrere la visualizzazione verso sinistra per tornare alle impostazioni della macchina virtuale, dato che è stata aperta una nuova finestra a destra, e fare scorrere la finestra per visualizzarla.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-137">You can also slide the view to the left to get back to the VM settings as it opened a new window off to the right and slid the window over to view it.</span></span>

1. <span data-ttu-id="6d2fa-138">Immettere un **nomeutente** che verrà usato per accedere con SSH.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-138">Enter a **username** you'll use to sign in with SSH.</span></span>

1. <span data-ttu-id="6d2fa-139">Nella sezione **Accesso amministratore** selezionare l'opzione per la chiave pubblica SSH in **Tipo di autenticazione**.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-139">In the **ADMINISTRATOR ACCESS** section, select the SSH public key option for **Authentication type**.</span></span>

1. <span data-ttu-id="6d2fa-140">Copiare la chiave SSH dal file di chiave pubblica e incollarla nel campo **Chiave pubblica SSH**.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-140">Copy the SSH key from your public key file and paste it into the **SSH public key** field.</span></span>

    > [!IMPORTANT]
    > <span data-ttu-id="6d2fa-141">Quando si copia la chiave pubblica nel portale di Azure, assicurarsi di non aggiungere spazi o caratteri di avanzamento riga.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-141">When you copy the public key into the Azure portal, make sure not to add any additional whitespace or line-feed characters.</span></span>

1. <span data-ttu-id="6d2fa-142">Nella sezione **REGOLE PORTA IN INGRESSO** aprire l'elenco e deselezionare **Non sono state trovate porte in ingresso pubbliche**.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-142">In the **INBOUND PORT RULES** section, open the list and uncheck **No public inbound ports**.</span></span> <span data-ttu-id="6d2fa-143">Trattandosi di una macchina virtuale Linux, è necessario avere la possibilità di accedere alla macchina virtuale con SSH da remoto.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-143">Since this is a Linux VM, we want to be able to access the VM using SSH remotely.</span></span> <span data-ttu-id="6d2fa-144">Scorrere l'elenco, se necessario, fino a trovare **SSH (22)** e selezionare questa opzione.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-144">Scroll the list if necessary until you find **SSH (22)** and select it.</span></span> <span data-ttu-id="6d2fa-145">Come indicato nella nota nell'interfaccia utente, è anche possibile modificare le porte di rete dopo aver creato la macchina virtuale.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-145">As the note in the UI indicates, we can also adjust the network ports after we create the VM.</span></span>

    ![Screenshot del portale di Azure con l'account amministratore e le impostazioni delle porte in ingresso descritte per aprire una macchina virtuale Linux per l'accesso SSH.](../media/3-open-ports.png)

## <a name="configure-disks-for-the-vm"></a><span data-ttu-id="6d2fa-147">Configurare i dischi per la macchina virtuale</span><span class="sxs-lookup"><span data-stu-id="6d2fa-147">Configure disks for the VM</span></span>

1. <span data-ttu-id="6d2fa-148">Fare clic su **Avanti: Dischi >** per passare alla sezione **Dischi**.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-148">Click **Next: Disks >** to move to the **Disks** section.</span></span>

    ![Screenshot del portale di Azure con la sezione Dischi del pannello Crea macchina virtuale.](../media/3-configure-disks.png)

1. <span data-ttu-id="6d2fa-150">Scegliere **SSD Premium** per **Tipo di disco del sistema operativo**.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-150">Choose **Premium SSD** for the **OS disk type**.</span></span>

1. <span data-ttu-id="6d2fa-151">Usare dischi gestiti, per non dover usare account di archiviazione.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-151">Use managed disks, so we don't have to work with storage accounts.</span></span> <span data-ttu-id="6d2fa-152">Se lo si desidera, è possibile modificare l'opzione nella GUI per visualizzare le differenze per le informazioni richieste da Azure.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-152">You can flip the switch in the GUI to see the difference in information that Azure needs if you like.</span></span>

### <a name="create-a-data-disk"></a><span data-ttu-id="6d2fa-153">Creare un disco dati</span><span class="sxs-lookup"><span data-stu-id="6d2fa-153">Create a data disk</span></span>

<span data-ttu-id="6d2fa-154">È importante ricordare che si otterranno un disco del sistema operativo (dev/sda) e un disco temporaneo (dev/sdb).</span><span class="sxs-lookup"><span data-stu-id="6d2fa-154">Recall that we will get an OS disk (/dev/sda) and a temporary disk (/dev/sdb).</span></span> <span data-ttu-id="6d2fa-155">Aggiungere anche un disco dati:</span><span class="sxs-lookup"><span data-stu-id="6d2fa-155">Let's add a data disk as well:</span></span>

1. <span data-ttu-id="6d2fa-156">Fare clic sul collegamento **Create and attach a new disk** (Crea e collega un nuovo disco) nella sezione **DISCHI DATI**.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-156">Click the **Create and attach a new disk** link in the **DATA DISKS** section.</span></span>

    ![Screenshot del portale di Azure che mostra il pannello Create a new disk (Crea un nuovo disco).](../media/3-add-data-disk.png)

1. <span data-ttu-id="6d2fa-158">È possibile accettare tutte le impostazioni predefinite: SSD Premium, 1023 GB e **Nessuno** (disco vuoto). Si noti, tuttavia, che questa è la posizione in cui si potrebbe usare uno snapshot o un archivio BLOB di Azure per creare un disco rigido virtuale.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-158">You can take all the defaults: Premium SSD, 1023 GB, and **None** (empty disk), although notice that here is where we could use a snapshot or Azure Blob storage to create a VHD.</span></span>

1. <span data-ttu-id="6d2fa-159">Fare clic su **OK** per creare il disco e tornare alla sezione **DISCHI DATI**.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-159">Click **OK** to create the disk and go back to the **DATA DISKS** section.</span></span>

1. <span data-ttu-id="6d2fa-160">A questo punto, nella prima riga sarà presente un nuovo disco.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-160">There should now be a new disk in the first row.</span></span>

    ![Screenshot del portale di Azure con la riga del disco dati appena creato per il processo di creazione della macchina virtuale.](../media/3-new-disk.png)

## <a name="configure-the-network"></a><span data-ttu-id="6d2fa-162">Configurare la rete</span><span class="sxs-lookup"><span data-stu-id="6d2fa-162">Configure the network</span></span>

1. <span data-ttu-id="6d2fa-163">Fare clic su **Avanti: Rete >** per passare alla sezione **Rete**.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-163">Click **Next: Networking >** to move to the **Networking** section.</span></span>

1. <span data-ttu-id="6d2fa-164">In un sistema di produzione in cui sono già presenti altri componenti è consigliabile usare una rete virtuale _esistente_.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-164">In a production system where we already have other components, we'd want to utilize an _existing_ virtual network.</span></span> <span data-ttu-id="6d2fa-165">In questo modo, la macchina virtuale può comunicare con altri servizi cloud nella soluzione.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-165">That way, our VM can communicate with the other cloud services in our solution.</span></span> <span data-ttu-id="6d2fa-166">Se non è già stata definita una rete virtuale per questa località, è possibile crearla e configurare gli elementi seguenti:</span><span class="sxs-lookup"><span data-stu-id="6d2fa-166">If there isn't one defined in this location yet, we can create it here and configure the:</span></span>
    - <span data-ttu-id="6d2fa-167">**Spazio di indirizzi**: spazio IPV4 complessivo disponibile per questa rete.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-167">**Address space**: The overall IPV4 space available to this network.</span></span>
    - <span data-ttu-id="6d2fa-168">**Intervallo IP subnet**: prima subnet in cui suddividere lo spazio di indirizzi. Deve rientrare nello spazio di indirizzi definito.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-168">**Subnet range**: The first subnet to subdivide the address space - it must fit within the defined address space.</span></span> <span data-ttu-id="6d2fa-169">Dopo aver creato la rete virtuale, è possibile aggiungere altre subnet.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-169">Once the VNet is created, you can add additional subnets.</span></span>

> [!NOTE]
> <span data-ttu-id="6d2fa-170">Per impostazione predefinita, Azure creerà una rete virtuale, l'interfaccia di rete e l'indirizzo IP pubblico per la macchina virtuale.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-170">By default, Azure will create a virtual network, network interface, and public IP for your VM.</span></span> <span data-ttu-id="6d2fa-171">Poiché non è facile modificare le opzioni di rete dopo aver creato la macchina virtuale, controllare sempre con attenzione le assegnazioni di rete per i servizi creati in Azure.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-171">It's not trivial to change the networking options after the VM has been created, so always double-check the network assignments on services you create in Azure.</span></span>

## <a name="finish-configuring-the-vm-and-create-the-image"></a><span data-ttu-id="6d2fa-172">Completare la configurazione della macchina virtuale e creare l'immagine</span><span class="sxs-lookup"><span data-stu-id="6d2fa-172">Finish configuring the VM and create the image</span></span>

<span data-ttu-id="6d2fa-173">Le opzioni rimanenti hanno impostazioni predefinite ragionevoli che non è necessario modificare.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-173">The rest of the options have reasonable defaults, and there's no need to change any of them.</span></span> <span data-ttu-id="6d2fa-174">Se lo si desidera, è possibile esplorare le altre schede.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-174">You can explore the other tabs if you like.</span></span> <span data-ttu-id="6d2fa-175">Accanto alle singole opzioni è disponibile un'icona `(i)` che permette di visualizzare un suggerimento con una spiegazione dell'opzione.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-175">The individual options have an `(i)` icon next to them that will show a help tip to explain the option.</span></span> <span data-ttu-id="6d2fa-176">Questo è un ottimo modo per ottenere informazioni sulle varie opzioni che è possibile usare per configurare la macchina virtuale:</span><span class="sxs-lookup"><span data-stu-id="6d2fa-176">This is a great way to learn about the various options you can use to configure the VM:</span></span>

1. <span data-ttu-id="6d2fa-177">Fare clic sul pulsante **Rivedi e crea** nella parte inferiore del pannello.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-177">Click the **Review + create** button at the bottom of the panel.</span></span>

1. <span data-ttu-id="6d2fa-178">Il sistema convaliderà le opzioni e fornirà informazioni dettagliate sulla macchina virtuale in fase di creazione.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-178">The system will validate your options and give you details about the VM being created.</span></span>

1. <span data-ttu-id="6d2fa-179">Fare clic su **Crea** per creare e distribuire la macchina virtuale.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-179">Click **Create** to create and deploy the VM.</span></span> <span data-ttu-id="6d2fa-180">Il dashboard di Azure mostrerà la macchina virtuale da distribuire.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-180">The Azure dashboard will show the VM that's being deployed.</span></span> <span data-ttu-id="6d2fa-181">Questa operazione può richiedere alcuni minuti.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-181">This may take several minutes.</span></span>

<span data-ttu-id="6d2fa-182">Durante la distribuzione, si esaminerà che cosa si può fare con questa macchina virtuale.</span><span class="sxs-lookup"><span data-stu-id="6d2fa-182">While that's deploying, let's look at what we can do with this VM.</span></span>