### <a name="exercise-3-train-a-tensorflow-model"></a><span data-ttu-id="ef819-101">Esercizio 3: Eseguire il training di un modello TensorFlow</span><span class="sxs-lookup"><span data-stu-id="ef819-101">Exercise 3: Train a TensorFlow model</span></span>

<span data-ttu-id="ef819-102">In questo esercizio, si eseguirà il training di un modello di classificazione delle immagini compilato con [TensorFlow](https://www.tensorflow.org/) per riconoscere le immagini che contengono hot dog.</span><span class="sxs-lookup"><span data-stu-id="ef819-102">In this exercise, you will train an image-classification model built with [TensorFlow](https://www.tensorflow.org/) to recognize images that contain hot dogs.</span></span> <span data-ttu-id="ef819-103">Invece di creare il modello da zero, operazione che richiederebbe una quantità elevata di potenza di calcolo e decine o centinaia di migliaia di immagini, si personalizzerà un modello preesistente, una pratica nota come [transfer learning](https://en.wikipedia.org/wiki/Transfer_learning).</span><span class="sxs-lookup"><span data-stu-id="ef819-103">Rather than create the model from scratch, which would require vast amounts of computing power and tens or hundreds of thousands of images, you will customize a preexisting model, a practice known as [transfer learning](https://en.wikipedia.org/wiki/Transfer_learning).</span></span> <span data-ttu-id="ef819-104">La tecnica del transfer learning consente di raggiungere livelli elevati di precisione con un training di pochi minuti su un laptop o PC standard con poche decine di immagini.</span><span class="sxs-lookup"><span data-stu-id="ef819-104">Transfer learning allows you to achieve high levels of accuracy with as little as a few minutes of training time on a typical laptop or PC and as few as several dozen images.</span></span>

<span data-ttu-id="ef819-105">Nel contesto del deep learning, il transfer learning richiede di iniziare con una rete neurale profonda già sottoposta a training per eseguire la classificazione di immagini, per poi aggiungere un livello che personalizza la rete per il dominio specifico del problema, ad esempio, per classificare le immagini in due gruppi : quelle contenenti hot dog e quelle che non li contengono.</span><span class="sxs-lookup"><span data-stu-id="ef819-105">In the context of deep learning, transfer learning involves starting with a deep neural network that is pretrained to perform image classification and adding a layer that customizes the network for your problem domain — for example, to classify images into two groups: those that contain hot dogs, and those that do not.</span></span> <span data-ttu-id="ef819-106">Sono disponibili in più di 20 modelli di classificazione delle immagini di TensorFlow già sottoposti a training all'indirizzo <https://github.com/tensorflow/models/tree/master/research/slim#pre-trained-models.>. I modelli [Inception](https://arxiv.org/abs/1512.00567) e [ResNet](https://towardsdatascience.com/an-overview-of-resnet-and-its-variants-5281e2f56035) sono caratterizzati da una maggiore precisione e requisiti a livello di risorse proporzionalmente più alti, mentre i modelli MobileNet sono stati sviluppati per i dispositivi motivi per offrire compattezza ed efficienza a discapito della precisione.</span><span class="sxs-lookup"><span data-stu-id="ef819-106">More than 20 pretrained TensorFlow image-classification models are available at <https://github.com/tensorflow/models/tree/master/research/slim#pre-trained-models.> The [Inception](https://arxiv.org/abs/1512.00567) and [ResNet](https://towardsdatascience.com/an-overview-of-resnet-and-its-variants-5281e2f56035) models are characterized by higher accuracy and commensurately higher resource requirements, while the MobileNet models trade accuracy for compactness and power efficiency and were developed with mobile devices in mind.</span></span> <span data-ttu-id="ef819-107">Tutti questi modelli sono ben noti alla community del deep learning e sono stati usati in numerose competizioni, così come in applicazioni reali.</span><span class="sxs-lookup"><span data-stu-id="ef819-107">All of these models are well known in the deep-learning community and have been used in a number of competitions as well as in real-world applications.</span></span> <span data-ttu-id="ef819-108">Si userà uno dei modelli MobileNet come base per la rete neurale, in modo da ottenere un equilibrio ragionevole tra precisione e tempi di training.</span><span class="sxs-lookup"><span data-stu-id="ef819-108">You will use one of the MobileNet models as the basis for your neural network in order to strike a reasonable balance between accuracy and training time.</span></span>

<span data-ttu-id="ef819-109">Il training del modello prevede poco di più che eseguire uno script Python che scarica il modello di base e aggiunge un livello sottoposto a training con le immagini e le etichette specifiche del dominio.</span><span class="sxs-lookup"><span data-stu-id="ef819-109">Training the model involves little more than running a Python script that downloads the base model and adds a layer trained with domain-specific images and labels.</span></span> <span data-ttu-id="ef819-110">Lo script necessario è disponibile in GitHub e le immagini che verranno usate sono state raccolte da migliaia di immagini di cibo di dominio pubblico disponibili da [Kaggle](https://www.kaggle.com).</span><span class="sxs-lookup"><span data-stu-id="ef819-110">The script you need is available on GitHub, and the images you will use were assembled from thousands of public-domain food images available from [Kaggle](https://www.kaggle.com).</span></span>

1. <span data-ttu-id="ef819-111">Nella Data Science VM fare clic sull'icona del Terminale nella parte inferiore della schermata per aprire una finestra del terminale.</span><span class="sxs-lookup"><span data-stu-id="ef819-111">In the Data Science VM, click the Terminal icon at the bottom of the screen to open a terminal window.</span></span>

    ![Avvio di una finestra del terminale](../images/launch-terminal.png)

    <span data-ttu-id="ef819-113">_Avvio di una finestra del terminale_</span><span class="sxs-lookup"><span data-stu-id="ef819-113">_Launching a terminal window_</span></span>

1. <span data-ttu-id="ef819-114">Eseguire il comando seguente nella finestra del terminale per passare alla cartella "notebooks":</span><span class="sxs-lookup"><span data-stu-id="ef819-114">Execute the following command in the terminal window to navigate to the "notebooks" folder:</span></span>

    ```bash
    cd notebooks
    ```
    <span data-ttu-id="ef819-115">Questa cartella viene prepopolata con i blocchi appunti di Jupyter di esempio preparati per la DSVM.</span><span class="sxs-lookup"><span data-stu-id="ef819-115">This folder is prepopulated with sample Jupyter notebooks curated for the DSVM.</span></span>

1. <span data-ttu-id="ef819-116">A questo punto è possibile usare il comando seguente per clonare il repository "TensorFlow for Poets" da GitHub:</span><span class="sxs-lookup"><span data-stu-id="ef819-116">Now use the following command to clone the "TensorFlow for Poets" repository from GitHub:</span></span>

    ```bash
    git clone https://github.com/googlecodelabs/tensorflow-for-poets-2
    ```
    > <span data-ttu-id="ef819-117">**Suggerimento**: è possibile copiare questa riga negli Appunti e quindi usare **MAIUSC+INS** per incollarla nella finestra del terminale.</span><span class="sxs-lookup"><span data-stu-id="ef819-117">**Tip**: You can copy this line to the clipboard, and then use **Shift+Ins** to paste it into the terminal window.</span></span>

    <span data-ttu-id="ef819-118">Questo repository contiene script per la creazione di modelli di transfer learning, richiamando un modello sottoposto a training per classificare un'immagine, e altro ancora.</span><span class="sxs-lookup"><span data-stu-id="ef819-118">This repo contains scripts for creating transfer-learning models, invoking a trained model in order to classify an image, and more.</span></span> <span data-ttu-id="ef819-119">Fa parte di [Google Codelabs](https://codelabs.developers.google.com/), che contiene un'ampia gamma di risorse e lab pratici per gli sviluppatori di software interessati ad approfondire TensorFlow e altri strumenti e API di Google.</span><span class="sxs-lookup"><span data-stu-id="ef819-119">It is part of [Google Codelabs](https://codelabs.developers.google.com/), which contains a variety of resources and hands-on labs for software developers interested in learning about TensorFlow and other Google tools and APIs.</span></span>

1. <span data-ttu-id="ef819-120">Una volta completata la clonazione, passare alla cartella contenente il modello clonato:</span><span class="sxs-lookup"><span data-stu-id="ef819-120">Once cloning is complete, navigate to the folder containing the cloned model:</span></span>

    ```bash
    cd tensorflow-for-poets-2
    ```

1. <span data-ttu-id="ef819-121">Usare il comando seguente per scaricare le immagini che verranno usate per il training del modello:</span><span class="sxs-lookup"><span data-stu-id="ef819-121">Use the following command to download the images that will be used to train the model:</span></span>

    ```bash
    wget https://topcs.blob.core.windows.net/public/tensorflow-resources.zip -O temp.zip; unzip temp.zip -d tf_files; rm temp.zip
    ```

    <span data-ttu-id="ef819-122">Questo comando scarica un file ZIP contenente centinaia di immagini di cibo (metà che includono hot dog e metà senza) e le copia nella sottodirectory denominata "tf_files."</span><span class="sxs-lookup"><span data-stu-id="ef819-122">This command downloads a zip file containing hundreds of food images — half containing hot dogs, and half that do not — and copies them into the subdirectory named "tf_files."</span></span>

1. <span data-ttu-id="ef819-123">Fare clic sull'icona di File Manager nella parte inferiore della schermata per aprire una finestra di File Manager.</span><span class="sxs-lookup"><span data-stu-id="ef819-123">Click the File Manager icon at the bottom of the screen to open a File Manager window.</span></span>

    ![Avvio di File Manager](../images/launch-file-manager.png)

    <span data-ttu-id="ef819-125">_Avvio di File Manager_</span><span class="sxs-lookup"><span data-stu-id="ef819-125">_Launching File Manager_</span></span>

1. <span data-ttu-id="ef819-126">In File Manager passare alla cartella "notebooks/tensorflow-for-poets-2/tf_files".</span><span class="sxs-lookup"><span data-stu-id="ef819-126">In File Manager, navigate to the "notebooks/tensorflow-for-poets-2/tf_files" folder.</span></span> <span data-ttu-id="ef819-127">Verificare che la cartella contenga una coppia di sottodirectory denominate "hot_dog" e "not_hot_dog."</span><span class="sxs-lookup"><span data-stu-id="ef819-127">Confirm that the folder contains a pair of subdirectories named "hot_dog" and "not_hot_dog."</span></span> <span data-ttu-id="ef819-128">La prima contiene numerose centinaia di immagini che includono hot dog, mentre la seconda contiene un numero uguale di immagini che **non** includono hot dog.</span><span class="sxs-lookup"><span data-stu-id="ef819-128">The former contains several hundred images containing hot dogs, while the latter contains an equal number of images that do **not** contain hot dogs.</span></span> <span data-ttu-id="ef819-129">Esplorare le immagini nella cartella "hot_dog" per farsi un'idea dell'aspetto di queste immagini.</span><span class="sxs-lookup"><span data-stu-id="ef819-129">Browse the images in the "hot_dog" folder to get a feel for what they look like.</span></span> <span data-ttu-id="ef819-130">Dare un'occhiata anche alle immagini nella cartella "not_hot_dog".</span><span class="sxs-lookup"><span data-stu-id="ef819-130">Check out the images in the "not_hot_dog" folder as well.</span></span>

    > <span data-ttu-id="ef819-131">Per eseguire il training di una rete neurale per determinare se un'immagine include un hot dog, si eseguirà il training sia con immagini che includono hot dog che con immagini che non li includono.</span><span class="sxs-lookup"><span data-stu-id="ef819-131">In order to train a neural network to determine whether an image contains a hot dog, you will train it with images that contain hot dogs as well as images that do not contain hot dogs.</span></span>

    ![Immagini nella cartella "hot_dog"](../images/hot-dog-images.png)

    <span data-ttu-id="ef819-133">*Immagini nella cartella "hot_dog"*</span><span class="sxs-lookup"><span data-stu-id="ef819-133">*Images in the "hot_dog" folder*</span></span>

    <span data-ttu-id="ef819-134">Verificare anche che la cartella contenga un file di testo denominato **retrained_labels_hotdog.txt**.</span><span class="sxs-lookup"><span data-stu-id="ef819-134">Also confirm that the folder contains a text file named **retrained_labels_hotdog.txt**.</span></span> <span data-ttu-id="ef819-135">Questo file identifica le sottodirectory contenenti le immagini di training</span><span class="sxs-lookup"><span data-stu-id="ef819-135">This file identifies the subdirectories containing the training images.</span></span> <span data-ttu-id="ef819-136">e viene usato dallo script di Python che esegue il training del modello.</span><span class="sxs-lookup"><span data-stu-id="ef819-136">It is used by the Python script that trains the model.</span></span> <span data-ttu-id="ef819-137">Lo script enumera i file in ogni sottodirectory identificati nel file di testo (il nome del file di testo è un parametro passato allo script) e usa questi file per il training della rete.</span><span class="sxs-lookup"><span data-stu-id="ef819-137">The script enumerates the files in each subdirectory identifed in the text file (the text file's name is a parameter passed to the script) and uses those files to train the network.</span></span>

1. <span data-ttu-id="ef819-138">Aprire una seconda finestra del terminale e passare alla cartella "notebooks/tensorflow-for-poets-2", ovvero la stessa aperta nella prima finestra del terminale.</span><span class="sxs-lookup"><span data-stu-id="ef819-138">Open a second terminal window and navigate to the "notebooks/tensorflow-for-poets-2" folder — the same one that is open in the first terminal window.</span></span> <span data-ttu-id="ef819-139">Usare quindi il comando seguente per avviare [TensorBoard](https://www.tensorflow.org/programmers_guide/summaries_and_tensorboard), ovvero un set di strumenti usati per visualizzare i modelli di TensorFlow e ottenere informazioni dettagliate per il processo di transfer learning:</span><span class="sxs-lookup"><span data-stu-id="ef819-139">Then use the following command to launch [TensorBoard](https://www.tensorflow.org/programmers_guide/summaries_and_tensorboard), which is a set of tools used to visualize TensorFlow models and gain insight into the transfer-learning process:</span></span>

     ```bash
     tensorboard --logdir tf_files/training_summaries
     ```

     > <span data-ttu-id="ef819-140">Questo comando avrà esito negativo se è già presente un'istanza di TensorBoard in esecuzione.</span><span class="sxs-lookup"><span data-stu-id="ef819-140">This command will fail if there is already an instance of TensorBoard running.</span></span> <span data-ttu-id="ef819-141">Se si riceve una notifica che la porta 6006 è già in uso, usare un comando ```pkill -f "tensorboard"``` per terminare il processo esistente.</span><span class="sxs-lookup"><span data-stu-id="ef819-141">If you are notified that port 6006 is already in use, use a ```pkill -f "tensorboard"``` command to kill the existing process.</span></span> <span data-ttu-id="ef819-142">Eseguire quindi di nuovo il comando ```tensorboard```.</span><span class="sxs-lookup"><span data-stu-id="ef819-142">Then execute the ```tensorboard``` command again.</span></span>

1. <span data-ttu-id="ef819-143">Tornare alla finestra del terminale originale ed eseguire i comandi seguenti:</span><span class="sxs-lookup"><span data-stu-id="ef819-143">Switch back to the original terminal window and execute the following commands:</span></span>

    ```bash
    IMAGE_SIZE=224;
    ARCHITECTURE="mobilenet_0.50_${IMAGE_SIZE}";
    ```

    <span data-ttu-id="ef819-144">Questi comandi inizializzano le variabili di ambiente specificando la risoluzione delle immagini di training e il modello di base su cui verrà creata la rete neurale.</span><span class="sxs-lookup"><span data-stu-id="ef819-144">These commands initialize environment variables specifying the resolution of the training images and the base model that your neural network will build upon.</span></span> <span data-ttu-id="ef819-145">I valori validi per IMAGE_SIZE sono 128, 160, 192 e 224.</span><span class="sxs-lookup"><span data-stu-id="ef819-145">Valid values for IMAGE_SIZE are 128, 160, 192, and 224.</span></span> <span data-ttu-id="ef819-146">Valori più alti aumentano il tempo di training, ma anche l'accuratezza del classificatore.</span><span class="sxs-lookup"><span data-stu-id="ef819-146">Higher values increase the training time, but also increase the accuracy of the classifier.</span></span>

1. <span data-ttu-id="ef819-147">A questo punto eseguire il comando seguente per avviare il processo di transfer learning, vale a dire, per eseguire il training del modello con le immagini scaricate:</span><span class="sxs-lookup"><span data-stu-id="ef819-147">Now execute the following command to start the transfer-learning process — that is, to train the model with the images you downloaded:</span></span>

    ```bash
    python scripts/retrain.py \
    --bottleneck_dir=tf_files/bottlenecks \
    --how_many_training_steps=500 \
    --model_dir=tf_files/models/ \
    --summaries_dir=tf_files/training_summaries/"${ARCHITECTURE}" \
    --output_graph=tf_files/retrained_graph_hotdog.pb \
    --output_labels=tf_files/retrained_labels_hotdog.txt \
    --architecture="${ARCHITECTURE}" \
    --image_dir=tf_files \
    --testing_percentage=15 \
    --validation_percentage=15
    ```

    <span data-ttu-id="ef819-148">**retrain.py** è uno degli script nel repository scaricato.</span><span class="sxs-lookup"><span data-stu-id="ef819-148">**retrain.py** is one of the scripts in the repo that you downloaded.</span></span> <span data-ttu-id="ef819-149">È complesso e composto da più di 1.000 righe di codice e commenti.</span><span class="sxs-lookup"><span data-stu-id="ef819-149">It is complex, comprising more than 1,000 lines of code and comments.</span></span> <span data-ttu-id="ef819-150">Il processo dello script prevede il download del modello specificato con l'opzione ```--architecture``` e l'aggiunta a tale modello di un nuovo livello sottoposto a training con le immagini disponibili nelle sottodirectory della directory specificata con l'opzione ```--image_dir```.</span><span class="sxs-lookup"><span data-stu-id="ef819-150">Its job is to download the model specified with the ```--architecture``` switch and add to it a new layer trained with the images found in subdirectories of the directory specified with the ```--image_dir``` switch.</span></span> <span data-ttu-id="ef819-151">Ogni immagine viene etichettata con il nome della sottodirectory in cui si trova, in questo caso, "hot_dog" o "not_hot_dog" e in questo modo la rete neurale modificata può classificare le immagini di input come immagini con hot-dog ("hot_dog") o immagini senza hot dog ("not_hot_dog").</span><span class="sxs-lookup"><span data-stu-id="ef819-151">Each image is labeled with the name of the subdirectory in which it is located — in this case, either "hot_dog" or "not_hot_dog" — enabling the modified neural network to classify images input to it as hot-dog images ("hot_dog") or not-hot-dog images ("not_hot_dog").</span></span> <span data-ttu-id="ef819-152">L'output della sessione di training è un file di modello TensorFlow denominato **retrained_graph_hotdog.pb**.</span><span class="sxs-lookup"><span data-stu-id="ef819-152">The output from the training session is a TensorFlow model file named **retrained_graph_hotdog.pb**.</span></span> <span data-ttu-id="ef819-153">Il nome e il percorso vengono specificati nell'opzione ```--output_graph```.</span><span class="sxs-lookup"><span data-stu-id="ef819-153">The name and location are specified in the ```--output_graph``` switch.</span></span>

1. <span data-ttu-id="ef819-154">Attendere il completamento del training, che dovrebbe richiedere meno di 5 minuti.</span><span class="sxs-lookup"><span data-stu-id="ef819-154">Wait for training to complete; it should take less than 5 minutes.</span></span> <span data-ttu-id="ef819-155">Controllare quindi l'output per determinare la precisione del modello.</span><span class="sxs-lookup"><span data-stu-id="ef819-155">Then check the output to determine the accuracy of the model.</span></span> <span data-ttu-id="ef819-156">I risultati possono variare leggermente rispetto a quello riportato di seguito, perché il processo di training include una quantità ridotta di stima casuale.</span><span class="sxs-lookup"><span data-stu-id="ef819-156">Your result may vary slightly from the one below because the training process involves a small amount of random estimation.</span></span>

      ![Valutazione della precisione del modello](../images/running-transfer-learning.png)

      <span data-ttu-id="ef819-158">_Valutazione della precisione del modello_</span><span class="sxs-lookup"><span data-stu-id="ef819-158">_Gauging the model's accuracy_</span></span>

1. <span data-ttu-id="ef819-159">Fare clic sull'icona del browser nella parte inferiore del desktop per aprire il browser installato nella Data Science VM.</span><span class="sxs-lookup"><span data-stu-id="ef819-159">Click the browser icon at the bottom of the desktop to open the browser installed in the Data Science VM.</span></span> <span data-ttu-id="ef819-160">Passare quindi a <http://0.0.0.0:6006> per connettersi a TensorBoard.</span><span class="sxs-lookup"><span data-stu-id="ef819-160">Then navigate to <http://0.0.0.0:6006> to connect to Tensorboard.</span></span>

    ![Avvio di Firefox](../images/launch-firefox.png)

    <span data-ttu-id="ef819-162">_Avvio di Firefox_</span><span class="sxs-lookup"><span data-stu-id="ef819-162">_Launching Firefox_</span></span>

1. <span data-ttu-id="ef819-163">Esaminare il grafico con l'etichetta "accuracy_1."</span><span class="sxs-lookup"><span data-stu-id="ef819-163">Inspect the graph labeled "accuracy_1."</span></span> <span data-ttu-id="ef819-164">La linea blu rappresenta la precisione ottenuta nel tempo durante l'esecuzione dei 500 passaggi di training specificati con l'opzione ```how_many_training_steps```.</span><span class="sxs-lookup"><span data-stu-id="ef819-164">The blue line depicts the accuracy achieved over time as the 500 training steps specified with the ```how_many_training_steps``` switch are executed.</span></span> <span data-ttu-id="ef819-165">Questa metrica è importante, perché mostra come la precisione del modello si evolva man mano che procede il training.</span><span class="sxs-lookup"><span data-stu-id="ef819-165">This metric is important, because it shows how the accuracy of the model evolves as training progresses.</span></span> <span data-ttu-id="ef819-166">Altrettanto importante è la distanza tra le linee blu e arancione che quantifica la quantità di overfitting che si è verificato e che dovrebbe essere sempre ridotto al minimo.</span><span class="sxs-lookup"><span data-stu-id="ef819-166">Equally important is the distance between the blue and orange lines, which quantifies the amount of overfitting that occurred and should always be minimized.</span></span> <span data-ttu-id="ef819-167">[Overfitting](https://en.wikipedia.org/wiki/Overfitting) significa che il modello è ideale per la classificazione delle immagini con il quale è stato sottoposto a training, ma non per la classificazione di altre immagini presentate.</span><span class="sxs-lookup"><span data-stu-id="ef819-167">[Overfitting](https://en.wikipedia.org/wiki/Overfitting) means the model is adept at classifying the images it was trained with, but not as adept at classifying other images presented to it.</span></span> <span data-ttu-id="ef819-168">In questo caso i risultati sono accettabili, perché esiste una differenza minore del 10% tra la linea arancione (la precisione di "training" ottenuta con le immagini di training) e la linea blu (la precisione di "convalida" ottenuta con il test con immagini all'esterno del set di training).</span><span class="sxs-lookup"><span data-stu-id="ef819-168">The results here are acceptable, because there is a difference of less than 10% between the orange line (the "training" accuracy achieved with the training images) and the blue line (the "validation" accuracy achieved when tested with images outside the training set).</span></span>

    ![Visualizzazione dei valori scalari di TensorBoard](../images/tensorboard-scalars.png)

    <span data-ttu-id="ef819-170">_Visualizzazione dei valori scalari di TensorBoard_</span><span class="sxs-lookup"><span data-stu-id="ef819-170">_The TensorBoard Scalars display_</span></span>

1. <span data-ttu-id="ef819-171">Fare clic su **GRAPHS** (Grafici) nel menu di TensorBoard ed esaminare il grafico visualizzato.</span><span class="sxs-lookup"><span data-stu-id="ef819-171">Click **GRAPHS** in the TensorBoard menu and inspect the graph shown there.</span></span> <span data-ttu-id="ef819-172">Lo scopo principale di questo grafico è rappresentare la rete neurale e i livelli che la compongono.</span><span class="sxs-lookup"><span data-stu-id="ef819-172">The primary purpose of this graph is to depict the neural network and the layers that comprise it.</span></span> <span data-ttu-id="ef819-173">In questo esempio, "input_1" è il livello che è stato sottoposto a training con le immagini di cibo e aggiunto alla rete.</span><span class="sxs-lookup"><span data-stu-id="ef819-173">In this example, "input_1" is the layer that was trained with food images and added to the network.</span></span> <span data-ttu-id="ef819-174">"MobilenetV1" è la rete neurale di base di partenza.</span><span class="sxs-lookup"><span data-stu-id="ef819-174">"MobilenetV1" is the base neural network that you started with.</span></span> <span data-ttu-id="ef819-175">Contiene molti livelli che non sono visualizzati.</span><span class="sxs-lookup"><span data-stu-id="ef819-175">It contains many layers which aren't shown.</span></span> <span data-ttu-id="ef819-176">Se si fosse creata una rete neurale dettagliata da zero, tutti i livelli sarebbero rappresentati in questo grafico.</span><span class="sxs-lookup"><span data-stu-id="ef819-176">Had you built a deep neural network from scratch, all of the layers would have been diagrammed here.</span></span> <span data-ttu-id="ef819-177">(Se si desidera vedere i livelli che costituiscono MobileNet, fare doppio clic sul blocco "MobilenetV1" nel diagramma.) Per altre informazioni sulla visualizzazione Graphs (Grafici) e le informazioni presentate, vedere [TensorBoard: Graph Visualization](https://www.tensorflow.org/programmers_guide/graph_viz) (TensorBoard: visualizzazione dei grafici).</span><span class="sxs-lookup"><span data-stu-id="ef819-177">(If you would like to see the layers that comprise the MobileNet, double-click the "MobilenetV1" block in the diagram.) For more information on the Graphs display and the information surfaced there, refer to [TensorBoard: Graph Visualization](https://www.tensorflow.org/programmers_guide/graph_viz).</span></span>

    ![Visualizzazione Graphs (Grafici) di TensorBoard](../images/tensorboard-graphs.png)

    <span data-ttu-id="ef819-179">_Visualizzazione Graphs (Grafici) di TensorBoard_</span><span class="sxs-lookup"><span data-stu-id="ef819-179">_The TensorBoard Graphs display_</span></span>

1. <span data-ttu-id="ef819-180">Tornare a File Manager e passare alla cartella "notebooks/tensorflow-for-poets-2/tf_files".</span><span class="sxs-lookup"><span data-stu-id="ef819-180">Switch back to File Manager and navigate to the "notebooks/tensorflow-for-poets-2/tf_files" folder.</span></span> <span data-ttu-id="ef819-181">Verificare che contenga un file denominato **retrained_graph_hotdog.pb**.</span><span class="sxs-lookup"><span data-stu-id="ef819-181">Confirm that it contains a file named **retrained_graph_hotdog.pb**.</span></span> <span data-ttu-id="ef819-182">*Questo file è stato creato durante il processo di training e contiene il modello TensorFlow sottoposto a training*.</span><span class="sxs-lookup"><span data-stu-id="ef819-182">*This file was created during the training process and contains the trained TensorFlow model*.</span></span> <span data-ttu-id="ef819-183">Verrà usato nel prossimo esercizio per richiamare il modello dall'app NotHotDog.</span><span class="sxs-lookup"><span data-stu-id="ef819-183">You will use it in the next exercise to invoke the model from the NotHotDog app.</span></span>

<span data-ttu-id="ef819-184">Lo script eseguito nel passaggio 10 specifica 500 passaggi di training, che rappresentano un buon compromesso tra precisione e tempo necessario per il training.</span><span class="sxs-lookup"><span data-stu-id="ef819-184">The script that you executed in Step 10 specified 500 training steps, which strikes a balance between accuracy and the time required for training.</span></span> <span data-ttu-id="ef819-185">Se si preferisce, provare a eseguire di nuovo il training del modello con un valore ```how_many_training_steps``` più elevato, ad esempio 1000 o 2000.</span><span class="sxs-lookup"><span data-stu-id="ef819-185">If you would like, try training the model again with a higher ```how_many_training_steps``` value such as 1000 or 2000.</span></span> <span data-ttu-id="ef819-186">Un numero di passaggi più alto corrisponde in genere a una maggiore precisione, ma a discapito di tempi di training maggiori.</span><span class="sxs-lookup"><span data-stu-id="ef819-186">A higher step count generally results in higher accuracy, but at the expense of increased training time.</span></span> <span data-ttu-id="ef819-187">Prestare attenzione all'overfitting, che, come promemoria, è rappresentato dalla differenza tra le linee blu e arancione nella visualizzazione dei valori scalari di TensorBoard.</span><span class="sxs-lookup"><span data-stu-id="ef819-187">Watch out for overfitting, which, as a reminder, is represented by the difference between the orange and blue lines in TensorBoard's Scalars display.</span></span>