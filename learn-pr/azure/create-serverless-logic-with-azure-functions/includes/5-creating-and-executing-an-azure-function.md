<span data-ttu-id="3a618-101">Ora che abbiamo creato un'app per le funzioni, scopriamo come creare, configurare ed eseguire una funzione di Azure.</span><span class="sxs-lookup"><span data-stu-id="3a618-101">Now that we have a function app created, we'll learn how to build, configure and execute an Azure function.</span></span>

### <a name="triggers"></a><span data-ttu-id="3a618-102">Trigger</span><span class="sxs-lookup"><span data-stu-id="3a618-102">Triggers</span></span>
<span data-ttu-id="3a618-103">Le funzioni di Azure sono basate su eventi e vengono eseguite in risposta a un evento, ad esempio la ricezione di una richiesta HTTP o di un messaggio da aggiungere a una coda.</span><span class="sxs-lookup"><span data-stu-id="3a618-103">Azure functions are event driven and execute in response to an event, such as receiving an HTTP request or a message being added to a queue.</span></span> 

<span data-ttu-id="3a618-104">Il tipo di evento che avvia la funzione viene chiamato trigger.</span><span class="sxs-lookup"><span data-stu-id="3a618-104">The type of event that initiates the function is called a trigger.</span></span> <span data-ttu-id="3a618-105">Le funzioni di Azure richiedono che venga configurato almeno un trigger;</span><span class="sxs-lookup"><span data-stu-id="3a618-105">Azure functions require at least one trigger to be configured.</span></span> <span data-ttu-id="3a618-106">in caso contrario, non c'è modo di eseguire la funzione.</span><span class="sxs-lookup"><span data-stu-id="3a618-106">Otherwise we'd have no way of running the function.</span></span>

 <span data-ttu-id="3a618-107">Azure supporta un'ampia gamma di trigger, tra cui:</span><span class="sxs-lookup"><span data-stu-id="3a618-107">Azure supports a wide range of triggers, including:</span></span>
* <span data-ttu-id="3a618-108">HTTPTrigger</span><span class="sxs-lookup"><span data-stu-id="3a618-108">HTTPTrigger</span></span>
* <span data-ttu-id="3a618-109">TimerTrigger</span><span class="sxs-lookup"><span data-stu-id="3a618-109">TimerTrigger</span></span>
* <span data-ttu-id="3a618-110">Webhook di GitHub</span><span class="sxs-lookup"><span data-stu-id="3a618-110">GitHub webhook</span></span>
* <span data-ttu-id="3a618-111">CosmosDBTrigger</span><span class="sxs-lookup"><span data-stu-id="3a618-111">CosmosDBTrigger</span></span>
* <span data-ttu-id="3a618-112">BlobTrigger</span><span class="sxs-lookup"><span data-stu-id="3a618-112">BlobTrigger</span></span>
* <span data-ttu-id="3a618-113">QueueTrigger</span><span class="sxs-lookup"><span data-stu-id="3a618-113">QueueTrigger</span></span>
* <span data-ttu-id="3a618-114">EventHubTrigger</span><span class="sxs-lookup"><span data-stu-id="3a618-114">EventHubTrigger</span></span>
* <span data-ttu-id="3a618-115">ServiceBusQueueTrigger</span><span class="sxs-lookup"><span data-stu-id="3a618-115">ServiceBusQueueTrigger</span></span>
* <span data-ttu-id="3a618-116">ServiceBusTopicTrigger</span><span class="sxs-lookup"><span data-stu-id="3a618-116">ServiceBusTopicTrigger</span></span>

### <a name="bindings"></a><span data-ttu-id="3a618-117">Associazioni</span><span class="sxs-lookup"><span data-stu-id="3a618-117">Bindings</span></span>
<span data-ttu-id="3a618-118">Le associazioni delle funzioni di Azure sono una modalità dichiarativa per connettere i dati alla funzione a livello di programmazione.</span><span class="sxs-lookup"><span data-stu-id="3a618-118">Azure function bindings are a declarative way to connect to data to your function programmatically.</span></span>

<span data-ttu-id="3a618-119">Le associazioni sono la connessione ai servizi dati.</span><span class="sxs-lookup"><span data-stu-id="3a618-119">Bindings are the connection to your data services.</span></span> <span data-ttu-id="3a618-120">Una funzione di Azure con zero, una o più associazioni consente di accedere a più origini dati.</span><span class="sxs-lookup"><span data-stu-id="3a618-120">An Azure function with zero, one or more bindings allows you to access multiple data sources.</span></span> <span data-ttu-id="3a618-121">Queste associazioni sono definite anche associazioni di input o output o anche associazioni di lettura o scrittura.</span><span class="sxs-lookup"><span data-stu-id="3a618-121">You also define them as input or output bindings, or you can think of them as read or write bindings.</span></span>

<span data-ttu-id="3a618-122">Si supponga di disporre di un oggetto QueueTrigger che avvia una funzione quando un BLOB viene aggiunto a una coda.</span><span class="sxs-lookup"><span data-stu-id="3a618-122">Suppose you had a QueueTrigger that initiates a function when a blob is added to a queue.</span></span> <span data-ttu-id="3a618-123">Un'associazione BLOB di input viene usata per connettersi ad Archiviazione BLOB di Azure.</span><span class="sxs-lookup"><span data-stu-id="3a618-123">An input blob binding would be used to connect to Azure Blob storage.</span></span> 

<span data-ttu-id="3a618-124">Le associazioni di output vengono usate per archiviare i dati.</span><span class="sxs-lookup"><span data-stu-id="3a618-124">Output bindings are used to store data.</span></span> <span data-ttu-id="3a618-125">Queste associazioni inviano ad esempio il valore restituito della funzione all'archiviazione tabelle di Azure.</span><span class="sxs-lookup"><span data-stu-id="3a618-125">For instance, they send the return value of the function to Azure Table storage.</span></span>

<span data-ttu-id="3a618-126">Un elenco completo delle associazioni disponibili è incluso nella [documentazione di Azure](https://docs.microsoft.com/azure/azure-functions/functions-triggers-bindings#supported-bindings).</span><span class="sxs-lookup"><span data-stu-id="3a618-126">A full list of available bindings is available in the [Azure documentation](https://docs.microsoft.com/azure/azure-functions/functions-triggers-bindings#supported-bindings).</span></span>

### <a name="a-sample-trigger-and-binding-functionjson"></a><span data-ttu-id="3a618-127">Un trigger e un'associazione di esempio (function.json)</span><span class="sxs-lookup"><span data-stu-id="3a618-127">A sample trigger and binding (function.json)</span></span>
<span data-ttu-id="3a618-128">Questa è una definizione di esempio di un trigger e di un'associazione per una funzione.</span><span class="sxs-lookup"><span data-stu-id="3a618-128">This is a sample definition of a trigger and binding for a function.</span></span> <span data-ttu-id="3a618-129">Si noti che a seconda del tipo di associazione che si descrive, i valori di proprietà da impostare sono diversi.</span><span class="sxs-lookup"><span data-stu-id="3a618-129">You will note that depending on the type of binding you are describing, there are different property values that need to be set.</span></span> <span data-ttu-id="3a618-130">Ogni associazione presenta anche una direzione che definisce se si tratta di un'associazione di input o output.</span><span class="sxs-lookup"><span data-stu-id="3a618-130">Each binding also has a direction that defines whether it is an input or output binding.</span></span> <span data-ttu-id="3a618-131">I trigger sono sempre associazioni di input.</span><span class="sxs-lookup"><span data-stu-id="3a618-131">Triggers are always input bindings.</span></span> <span data-ttu-id="3a618-132">Questo esempio illustra una funzione attivata da un messaggio che viene aggiunto a una coda denominata **myqueue-items**.</span><span class="sxs-lookup"><span data-stu-id="3a618-132">This sample shows a function that is triggered by a message being added to a queue named **myqueue-items**.</span></span> <span data-ttu-id="3a618-133">Il valore restituito della funzione viene quindi inviato alla tabella **outTable** nell'archiviazione tabelle di Azure.</span><span class="sxs-lookup"><span data-stu-id="3a618-133">It then sends the return value of the function to the **outTable** table in Azure Table storage.</span></span>

```javascript
{
  "bindings": [
    {
      "name": "order",
      "type": "queueTrigger",
      "direction": "in",
      "queueName": "myqueue-items",
      "connection": "MY_STORAGE_ACCT_APP_SETTING"
    },
    {
      "name": "$return",
      "type": "table",
      "direction": "out",
      "tableName": "outTable",
      "connection": "MY_TABLE_STORAGE_ACCT_APP_SETTING"
    }
  ]
}
```
## <a name="premade-functions-and-templates"></a><span data-ttu-id="3a618-134">Funzioni e modelli predefiniti</span><span class="sxs-lookup"><span data-stu-id="3a618-134">Premade functions and templates</span></span>
<span data-ttu-id="3a618-135">Azure offre modelli preconfigurati per gli scenari di funzioni di Azure più comuni.</span><span class="sxs-lookup"><span data-stu-id="3a618-135">Azure provides pre-configured templates for common Azure function scenarios.</span></span>

### <a name="quickstart-templates"></a><span data-ttu-id="3a618-136">Modelli di Guida introduttiva</span><span class="sxs-lookup"><span data-stu-id="3a618-136">Quickstart templates</span></span>
<span data-ttu-id="3a618-137">Per aggiungere una funzione di Azure, è necessario selezionare un'app per le funzioni.</span><span class="sxs-lookup"><span data-stu-id="3a618-137">To add an Azure function, you must select a function app.</span></span> <span data-ttu-id="3a618-138">L'app per le funzioni può essere identificata dalla saetta presente nell'icona con parentesi a punta.</span><span class="sxs-lookup"><span data-stu-id="3a618-138">The function app can be identified by the lightning bolt within pointy brackets icon.</span></span>  
![Icona di funzione](../images/5-function-icon.png)

<span data-ttu-id="3a618-140">Quando si aggiunge la prima funzione di Azure, viene visualizzata la schermata di avvio rapido.</span><span class="sxs-lookup"><span data-stu-id="3a618-140">When adding your first Azure function, you are presented with the Quickstart screen.</span></span> <span data-ttu-id="3a618-141">Questa schermata consente selezionare il tipo di trigger e il linguaggio di programmazione desiderati.</span><span class="sxs-lookup"><span data-stu-id="3a618-141">This screen allows you to make a selection on your desired trigger type and programming language.</span></span> <span data-ttu-id="3a618-142">In base alle selezioni effettuate, Azure genera quindi automaticamente il codice della funzione e la configurazione.</span><span class="sxs-lookup"><span data-stu-id="3a618-142">Then, based on your selections, Azure will generate the function code and configuration for you.</span></span>  
![Avvio rapido della funzione](../images/5-quickstart-form.png)

### <a name="function-templates"></a><span data-ttu-id="3a618-144">Modelli di funzione</span><span class="sxs-lookup"><span data-stu-id="3a618-144">Function templates</span></span>
<span data-ttu-id="3a618-145">La selezione di modelli non è limitata a quelli disponibili nell'avvio rapido.</span><span class="sxs-lookup"><span data-stu-id="3a618-145">The selection of templates is not limited to those available in the Quickstart.</span></span> <span data-ttu-id="3a618-146">È possibile scegliere tra oltre 30 modelli diversi.</span><span class="sxs-lookup"><span data-stu-id="3a618-146">You also have the option of choosing from over 30 different templates.</span></span> <span data-ttu-id="3a618-147">È possibile accedere alla schermata di elenco dei modelli durante la creazione delle funzioni successive o selezionando l'opzione **Funzione personalizzata** nella schermata di avvio rapido.</span><span class="sxs-lookup"><span data-stu-id="3a618-147">You can access the template list screen while creating subsequent functions, or by selecting the **Custom function** option on the Quickstart screen.</span></span>  
<span data-ttu-id="3a618-148">![Modelli di funzione](../images/5-template-list.png)</span><span class="sxs-lookup"><span data-stu-id="3a618-148">![Function templates](../images/5-template-list.png)</span></span>

## <a name="navigating-to-your-function-and-files"></a><span data-ttu-id="3a618-149">Passaggio alla funzione e ai file</span><span class="sxs-lookup"><span data-stu-id="3a618-149">Navigating to your function and files</span></span>
<span data-ttu-id="3a618-150">Dopo aver creato una funzione da un modello, vengono creati numerosi file.</span><span class="sxs-lookup"><span data-stu-id="3a618-150">After you create a function from a template, several files are created.</span></span> <span data-ttu-id="3a618-151">Si supponga che avere scelto di usare l'Avvio rapito per Webhook + API usando JavaScript.</span><span class="sxs-lookup"><span data-stu-id="3a618-151">Suppose you opted to use the Webhook + API Quickstart using JavaScript.</span></span> <span data-ttu-id="3a618-152">I file generati sono un file di configurazione denominato **function.json** e un file del codice sorgente denominato **index.js**.</span><span class="sxs-lookup"><span data-stu-id="3a618-152">The files generated would be a configuration file, **function.json**, and a source code file, **index.js**.</span></span> <span data-ttu-id="3a618-153">Quando si accede all'app per le funzioni, viene visualizzata una struttura di menu che visualizza le funzioni che sono state create nell'app.</span><span class="sxs-lookup"><span data-stu-id="3a618-153">When accessing the function app, you will be presented with a menu tree displaying the functions that have been created in the app.</span></span> <span data-ttu-id="3a618-154">È anche in questa voce di menu **Funzioni** che si possono aggiungere altre funzioni all'app per le funzioni (un'icona con un segno + appare quando si passa il puntatore sulla voce di menu).</span><span class="sxs-lookup"><span data-stu-id="3a618-154">It is also on this **Functions** menu item that you are able to add additional functions to the function app (a plus + icon will appear when you're hovering over the menu item).</span></span>  
<span data-ttu-id="3a618-155">Pulsante ![Aggiungi funzione](../images/5-function-add-button.png)</span><span class="sxs-lookup"><span data-stu-id="3a618-155">![Add Function button](../images/5-function-add-button.png)</span></span> 

<span data-ttu-id="3a618-156">Nel caso dell'Avvio rapido summenzionato, quando si espande **Funzioni** nella visualizzazione albero, si vedrà una funzione con il nome predefinito **HttpTriggerJS1** (indicato anche dall'icona di funzione).</span><span class="sxs-lookup"><span data-stu-id="3a618-156">In the case of this Quickstart mentioned above, when you expand **Functions** in the tree view, you will see one function with a default name of **HttpTriggerJS1** (also indicated by the function f icon).</span></span> <span data-ttu-id="3a618-157">La selezione di questa funzione aprirà una finestra del codice e visualizzerà il file di origine **index.js**.</span><span class="sxs-lookup"><span data-stu-id="3a618-157">Selecting this function will open up a code window and display the **index.js** source file.</span></span> <span data-ttu-id="3a618-158">Sul lato destro della finestra del codice è presente un menu a comparsa che include una scheda per **visualizzare i file**.</span><span class="sxs-lookup"><span data-stu-id="3a618-158">On the right-hand side of the code window, there is a flyout menu that includes a tab to **View files**.</span></span> <span data-ttu-id="3a618-159">Se si seleziona questa scheda, viene visualizzata la struttura del file (riprodotta nell'archivio) che costituisce la funzione.</span><span class="sxs-lookup"><span data-stu-id="3a618-159">Selecting this tab will show you the file structure (mimicked in storage) that makes up your function.</span></span>  
<span data-ttu-id="3a618-160">![Modelli di funzione](../images/5-file-navigation.png)</span><span class="sxs-lookup"><span data-stu-id="3a618-160">![Function templates](../images/5-file-navigation.png)</span></span>

## <a name="execution-options-for-testing-your-azure-function"></a><span data-ttu-id="3a618-161">Opzioni di esecuzione per testare la funzione di Azure</span><span class="sxs-lookup"><span data-stu-id="3a618-161">Execution options for testing your Azure function</span></span>
<span data-ttu-id="3a618-162">Dopo avere creato una funzione di Azure, è necessario decidere come eseguirne il test.</span><span class="sxs-lookup"><span data-stu-id="3a618-162">Once you have an Azure function created, you will need to know how to test it.</span></span> <span data-ttu-id="3a618-163">Esistono due approcci: l'esecuzione manuale e il test nel portale di Azure.</span><span class="sxs-lookup"><span data-stu-id="3a618-163">There are a couple of approaches: manual execution and testing from within the Azure portal itself.</span></span>

### <a name="manual-execution-of-a-function"></a><span data-ttu-id="3a618-164">Esecuzione manuale di una funzione</span><span class="sxs-lookup"><span data-stu-id="3a618-164">Manual execution of a function</span></span>
<span data-ttu-id="3a618-165">È possibile avviare l'esecuzione di una funzione attivando manualmente il trigger configurato.</span><span class="sxs-lookup"><span data-stu-id="3a618-165">You can initiate the execution of a function by manually triggering the configured trigger.</span></span> <span data-ttu-id="3a618-166">Se ad esempio si usa un oggetto HTTPTrigger, è possibile usare uno strumento come Postman o cURL per avviare una richiesta HTTP all'URL dell'endpoint della funzione.</span><span class="sxs-lookup"><span data-stu-id="3a618-166">For instance, if you are using an HttpTrigger - you can use a tool such as Postman or cURL to initiate an HTTP request to your function endpoint URL.</span></span>  
<span data-ttu-id="3a618-167">![Esecuzione in Postman di una funzione](../images/5-postman-execution.png) È possibile ottenere l'URL dell'endpoint selezionando l'oggetto HTTPTrigger dal riquadro di navigazione di sinistra e quindi selezionando il pulsante **Get function URL** (Ottieni URL di funzione).</span><span class="sxs-lookup"><span data-stu-id="3a618-167">![Postman execution of a function](../images/5-postman-execution.png) You can obtain the endpoint URL by selecting your HTTP Trigger from the left navigation, and then selecting the **Get function URL** button.</span></span>  
<span data-ttu-id="3a618-168">![Get function URL](../images/5-get-function-url.png) (Ottieni URL di funzione)</span><span class="sxs-lookup"><span data-stu-id="3a618-168">![Get function URL](../images/5-get-function-url.png)</span></span>

### <a name="testing-a-function-in-the-azure-portal"></a><span data-ttu-id="3a618-169">Esecuzione del test di una funzione nel portale di Azure</span><span class="sxs-lookup"><span data-stu-id="3a618-169">Testing a function in the Azure portal</span></span>
<span data-ttu-id="3a618-170">Il portale di Azure offre un altro modo pratico per testare le funzioni.</span><span class="sxs-lookup"><span data-stu-id="3a618-170">Alternatively, the Azure portal also provides a convenient way to test your functions.</span></span> <span data-ttu-id="3a618-171">Sul lato destro della finestra del codice è presente un menu di spostamento a schede a comparsa.</span><span class="sxs-lookup"><span data-stu-id="3a618-171">On the right side of the code window, there is a flyout tabbed navigation menu.</span></span> <span data-ttu-id="3a618-172">In questo menu è presente la scheda **Test**.</span><span class="sxs-lookup"><span data-stu-id="3a618-172">This menu contains a **Test** item.</span></span> <span data-ttu-id="3a618-173">Se si espande il menu e si seleziona questa scheda, è possibile eseguire la funzione e visualizzarne il risultato.</span><span class="sxs-lookup"><span data-stu-id="3a618-173">Expanding the menu and selecting this tab gives you another way to execute your function and view the result.</span></span> <span data-ttu-id="3a618-174">In linea con lo scenario di HTTPTrigger, è possibile impostare il metodo HTTP e aggiungere i parametri di stringa di query e le intestazioni HTTP alla richiesta.</span><span class="sxs-lookup"><span data-stu-id="3a618-174">In keeping with the HttpTrigger scenario, you can set the HTTP method, and add querystring parameters and HTTP headers to the request.</span></span> <span data-ttu-id="3a618-175">È anche possibile modificare il corpo della richiesta per testare altri scenari.</span><span class="sxs-lookup"><span data-stu-id="3a618-175">You may also change the request body to test additional scenarios.</span></span> <span data-ttu-id="3a618-176">La finestra di output mostra il risultato della funzione.</span><span class="sxs-lookup"><span data-stu-id="3a618-176">The output window shows the result of the function.</span></span>  
<span data-ttu-id="3a618-177">![Esecuzione di una funzione nel portale](../images/5-portal-execution.png)</span><span class="sxs-lookup"><span data-stu-id="3a618-177">![Portal execution of a function](../images/5-portal-execution.png)</span></span>

## <a name="monitoring-an-azure-function"></a><span data-ttu-id="3a618-178">Monitoraggio di una funzione di Azure</span><span class="sxs-lookup"><span data-stu-id="3a618-178">Monitoring an Azure function</span></span>
<span data-ttu-id="3a618-179">Durante lo sviluppo di una funzione di Azure, la possibilità di registrare i messaggi e di determinare gli scenari di eccezione è essenziale per garantire che le funzioni create siano pronte per la produzione.</span><span class="sxs-lookup"><span data-stu-id="3a618-179">During Azure function development, being able to log messages and determine exception scenarios is critical to ensuring that you are getting your functions production-ready.</span></span> <span data-ttu-id="3a618-180">Questa possibilità è importante in uno scenario di produzione, quando si esegue il monitoraggio per risalire all'origine di un difetto.</span><span class="sxs-lookup"><span data-stu-id="3a618-180">This is just as important in a production scenario, when you are tracking down the source of a defect.</span></span> <span data-ttu-id="3a618-181">Il portale di Azure offre un'interfaccia utente che include un dashboard di monitoraggio, nonché un modo per esaminare i log e le eccezioni di esecuzione derivanti dalle funzioni di Azure create.</span><span class="sxs-lookup"><span data-stu-id="3a618-181">The Azure portal gives us a user interface that provides a monitoring dashboard as well as a way to review execution logs and exceptions obtained from your Azure functions.</span></span>

### <a name="monitoring-dashboard"></a><span data-ttu-id="3a618-182">Dashboard di monitoraggio</span><span class="sxs-lookup"><span data-stu-id="3a618-182">Monitoring dashboard</span></span>
<span data-ttu-id="3a618-183">Nel menu di spostamento dell'app per le funzioni, quando si espande il nodo della funzione, viene visualizzata la voce di menu **Monitor** (Monitora).</span><span class="sxs-lookup"><span data-stu-id="3a618-183">In the function app navigation menu, once you expand the function node - you will see a **Monitor** menu item.</span></span> <span data-ttu-id="3a618-184">Il dashboard di monitoraggio offre un modo rapido per visualizzare la cronologia delle esecuzioni della funzione.</span><span class="sxs-lookup"><span data-stu-id="3a618-184">The monitor dashboard provides a quick way to view the history of function executions.</span></span> <span data-ttu-id="3a618-185">Questa visualizzazione mostra il timestamp, il codice di risultato, la durata e l'ID operazione, nonché un messaggio che indica se la funzione è stata completata o meno correttamente.</span><span class="sxs-lookup"><span data-stu-id="3a618-185">This view displays the timestamp, result code, duration, and operation ID, as well as if it completed successfully.</span></span> <span data-ttu-id="3a618-186">I dati vengono popolati tramite Azure Application Insights.</span><span class="sxs-lookup"><span data-stu-id="3a618-186">The data is populated via Azure Application Insights.</span></span>  
<span data-ttu-id="3a618-187">![Monitoraggio della funzione](../images/5-monitor-function.png)</span><span class="sxs-lookup"><span data-stu-id="3a618-187">![Function monitor](../images/5-monitor-function.png)</span></span>

### <a name="log-window"></a><span data-ttu-id="3a618-188">Finestra registro</span><span class="sxs-lookup"><span data-stu-id="3a618-188">Log window</span></span>
<span data-ttu-id="3a618-189">È possibile aggiungere anche istruzioni di registrazione alla funzione.</span><span class="sxs-lookup"><span data-stu-id="3a618-189">You are also able to add log statements to your function.</span></span> <span data-ttu-id="3a618-190">Queste istruzioni vengono visualizzate nella finestra registro della funzione.</span><span class="sxs-lookup"><span data-stu-id="3a618-190">These statements will appear in the log window of the function.</span></span> <span data-ttu-id="3a618-191">La finestra registro si trova nel menu a schede a comparsa nella parte inferiore della finestra del codice.</span><span class="sxs-lookup"><span data-stu-id="3a618-191">The log window is located in a tabbed flyout menu located at the bottom of the code window.</span></span> <span data-ttu-id="3a618-192">Quando si usa una funzione basata su JavaScript, è necessario aggiungere un'istruzione di registrazione usando la sintassi seguente:</span><span class="sxs-lookup"><span data-stu-id="3a618-192">When using a JavaScript-based function, you would add a log statement using the following syntax:</span></span>
```javascript
  context.log('Enter your logging statement here');
```  
![Finestra registro](../images/5-log-window.png)

### <a name="errors-and-warnings-window"></a><span data-ttu-id="3a618-194">Finestra degli errori e degli avvisi</span><span class="sxs-lookup"><span data-stu-id="3a618-194">Errors and warnings window</span></span>
<span data-ttu-id="3a618-195">La scheda della finestra degli errori e degli avvisi si trova nello stesso menu a comparsa della finestra registro.</span><span class="sxs-lookup"><span data-stu-id="3a618-195">You can locate the errors and warnings window tab in the same flyout menu as the log window.</span></span> <span data-ttu-id="3a618-196">In questa finestra vengono visualizzati gli errori e gli avvisi di compilazione all'interno del codice.</span><span class="sxs-lookup"><span data-stu-id="3a618-196">This window will show compilation errors and warnings within your code.</span></span> <span data-ttu-id="3a618-197">Poiché JavaScript è un linguaggio dinamico e interpretato, l'immagine seguente mostra un errore di compilazione in una funzione C#.</span><span class="sxs-lookup"><span data-stu-id="3a618-197">As JavaScript is a dynamic and interpreted language, the following image shows a compilation error in a C# function.</span></span>  
![Finestra degli errori e degli avvisi](../images/5-errors-window.png)
