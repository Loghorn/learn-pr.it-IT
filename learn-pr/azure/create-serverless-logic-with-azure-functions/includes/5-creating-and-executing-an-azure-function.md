<span data-ttu-id="634bf-101">Dopo aver creato un'app per le funzioni, si apprenderà come compilare, configurare ed eseguire una funzione di Azure.</span><span class="sxs-lookup"><span data-stu-id="634bf-101">Now that we have a function app created, we'll learn how to build, configure, and execute an Azure function.</span></span>

### <a name="triggers"></a><span data-ttu-id="634bf-102">Trigger</span><span class="sxs-lookup"><span data-stu-id="634bf-102">Triggers</span></span>

<span data-ttu-id="634bf-103">Le funzioni sono basate sugli eventi, ovvero vengono eseguite in risposta a un evento.</span><span class="sxs-lookup"><span data-stu-id="634bf-103">Functions are event driven, which means they run in response to an event.</span></span>

<span data-ttu-id="634bf-104">Il tipo di evento che avvia la funzione viene chiamato *trigger*.</span><span class="sxs-lookup"><span data-stu-id="634bf-104">The type of event that starts the function is called a *trigger*.</span></span> <span data-ttu-id="634bf-105">Si configura una funzione con un solo trigger.</span><span class="sxs-lookup"><span data-stu-id="634bf-105">You configure a function with exactly one trigger.</span></span>

 <span data-ttu-id="634bf-106">Azure supporta i trigger per i servizi seguenti.</span><span class="sxs-lookup"><span data-stu-id="634bf-106">Azure supports triggers for the following services.</span></span>


|<span data-ttu-id="634bf-107">Servizio</span><span class="sxs-lookup"><span data-stu-id="634bf-107">Service</span></span>  |<span data-ttu-id="634bf-108">Descrizione del trigger</span><span class="sxs-lookup"><span data-stu-id="634bf-108">Trigger description</span></span>  |
|---------|---------|
|<span data-ttu-id="634bf-109">Archiviazione BLOB</span><span class="sxs-lookup"><span data-stu-id="634bf-109">Blob storage</span></span>     |  <span data-ttu-id="634bf-110">Avvia una funzione quando viene rilevato un BLOB nuovo o aggiornato.</span><span class="sxs-lookup"><span data-stu-id="634bf-110">Start a function when a new or updated blob is detected.</span></span>       |
|<span data-ttu-id="634bf-111">Cosmos DB</span><span class="sxs-lookup"><span data-stu-id="634bf-111">Cosmos DB</span></span>     |  <span data-ttu-id="634bf-112">Avvia una funzione quando vengono rilevati inserimenti e aggiornamenti.</span><span class="sxs-lookup"><span data-stu-id="634bf-112">Start a function when inserts and updates are detected.</span></span>      |
|<span data-ttu-id="634bf-113">Griglia di eventi</span><span class="sxs-lookup"><span data-stu-id="634bf-113">Event Grid</span></span>     |   <span data-ttu-id="634bf-114">Avvia una funzione quando viene ricevuto un evento da Griglia di eventi.</span><span class="sxs-lookup"><span data-stu-id="634bf-114">Start a function when an event is received from Event Grid.</span></span>       |
|<span data-ttu-id="634bf-115">HTTP</span><span class="sxs-lookup"><span data-stu-id="634bf-115">HTTP</span></span>     |   <span data-ttu-id="634bf-116">Avvia una funzione con una richiesta HTTP.</span><span class="sxs-lookup"><span data-stu-id="634bf-116">Start a function with an HTTP request.</span></span>      |
|<span data-ttu-id="634bf-117">Eventi di Microsoft Graph</span><span class="sxs-lookup"><span data-stu-id="634bf-117">Microsoft Graph Events</span></span>     |  <span data-ttu-id="634bf-118">Avvia una funzione in risposta a un webhook in ingresso di Microsoft Graph.</span><span class="sxs-lookup"><span data-stu-id="634bf-118">Start a function in response to an incoming webhook from the Microsoft Graph.</span></span> <span data-ttu-id="634bf-119">Ogni istanza del trigger può rispondere a un tipo di risorsa di Microsoft Graph.</span><span class="sxs-lookup"><span data-stu-id="634bf-119">Each instance of this trigger can react to one Microsoft Graph resource type.</span></span>       |
|<span data-ttu-id="634bf-120">Archiviazione code</span><span class="sxs-lookup"><span data-stu-id="634bf-120">Queue storage</span></span>     |    <span data-ttu-id="634bf-121">Avvia una funzione quando viene ricevuto un nuovo elemento in una coda.</span><span class="sxs-lookup"><span data-stu-id="634bf-121">Start a function when a new item is received on a queue.</span></span> <span data-ttu-id="634bf-122">Il messaggio in coda viene inviato come input alla funzione.</span><span class="sxs-lookup"><span data-stu-id="634bf-122">The queue message is provided as input to the function.</span></span>      |
|<span data-ttu-id="634bf-123">Bus di servizio</span><span class="sxs-lookup"><span data-stu-id="634bf-123">Service Bus</span></span>     |  <span data-ttu-id="634bf-124">Avvia una funzione in risposta ai messaggi di una coda del bus di servizio.</span><span class="sxs-lookup"><span data-stu-id="634bf-124">Start a function in response to messages from a Service Bus queue.</span></span>       |
|<span data-ttu-id="634bf-125">Timer</span><span class="sxs-lookup"><span data-stu-id="634bf-125">Timer</span></span>     |  <span data-ttu-id="634bf-126">Avvia una funzione in una pianificazione.</span><span class="sxs-lookup"><span data-stu-id="634bf-126">Start a function on a schedule.</span></span>       |
|<span data-ttu-id="634bf-127">Webhook</span><span class="sxs-lookup"><span data-stu-id="634bf-127">Webhooks</span></span>     |  <span data-ttu-id="634bf-128">Avvia una funzione con una richiesta webhook.</span><span class="sxs-lookup"><span data-stu-id="634bf-128">Start a function with a webhook request.</span></span>       |

### <a name="bindings"></a><span data-ttu-id="634bf-129">Associazioni</span><span class="sxs-lookup"><span data-stu-id="634bf-129">Bindings</span></span>

<span data-ttu-id="634bf-130">Le associazioni delle funzioni di Azure sono una modalità dichiarativa per connettere dati e servizi alla funzione.</span><span class="sxs-lookup"><span data-stu-id="634bf-130">Azure Functions bindings are a declarative way to connect data and services to your function.</span></span> <span data-ttu-id="634bf-131">Non è necessario scrivere codice nella funzione per connettersi alle origini dati e gestire le connessioni.</span><span class="sxs-lookup"><span data-stu-id="634bf-131">You don't have to write code in your function to connect to data sources and manage connections.</span></span> <span data-ttu-id="634bf-132">La piattaforma risolve automaticamente questa complessità.</span><span class="sxs-lookup"><span data-stu-id="634bf-132">The platform takes care that complexity for you.</span></span> <span data-ttu-id="634bf-133">Un'associazione ha una direzione.</span><span class="sxs-lookup"><span data-stu-id="634bf-133">A binding has a direction.</span></span> <span data-ttu-id="634bf-134">Il codice legge i dati dalle associazioni di *input* e scrive i dati nelle associazioni di *output*.</span><span class="sxs-lookup"><span data-stu-id="634bf-134">Your code reads data from *input* bindings and writes data to *output* bindings.</span></span> <span data-ttu-id="634bf-135">Ecco un esempio.</span><span class="sxs-lookup"><span data-stu-id="634bf-135">Let's look at an example.</span></span>

<span data-ttu-id="634bf-136">Per leggere dati dall'archiviazione BLOB, si configura un'associazione di input di tipo *blob*.</span><span class="sxs-lookup"><span data-stu-id="634bf-136">To read data from Blob storage, you configure an input binding of type *blob*.</span></span> <span data-ttu-id="634bf-137">Per scrivere un messaggio all'archiviazione code, si configura un'associazione di output di tipo *queue*.</span><span class="sxs-lookup"><span data-stu-id="634bf-137">To write a message to Queue storage, you configure an output binding of type *queue*.</span></span>

<span data-ttu-id="634bf-138">Per altre informazioni sulle associazioni e i trigger supportati, consultare la [documentazione di Azure](https://docs.microsoft.com/azure/azure-functions/functions-triggers-bindings#supported-bindings).</span><span class="sxs-lookup"><span data-stu-id="634bf-138">For more information about the supported bindings and triggers, check out the [Azure documentation](https://docs.microsoft.com/azure/azure-functions/functions-triggers-bindings#supported-bindings).</span></span>

### <a name="a-sample-trigger-and-binding-functionjson"></a><span data-ttu-id="634bf-139">Un trigger e un'associazione di esempio (function.json)</span><span class="sxs-lookup"><span data-stu-id="634bf-139">A sample trigger and binding (function.json)</span></span>

<span data-ttu-id="634bf-140">Il file JSON seguente è una definizione di esempio di un trigger e di un'associazione per una funzione.</span><span class="sxs-lookup"><span data-stu-id="634bf-140">The following JSON is sample definition of a trigger and binding for a function.</span></span>

```javascript
{
  "bindings": [
    {
      "name": "order",
      "type": "queueTrigger",
      "direction": "in",
      "queueName": "myqueue-items",
      "connection": "MY_STORAGE_ACCT_APP_SETTING"
    },
    {
      "name": "$return",
      "type": "table",
      "direction": "out",
      "tableName": "outTable",
      "connection": "MY_TABLE_STORAGE_ACCT_APP_SETTING"
    }
  ]
}
```

<span data-ttu-id="634bf-141">Come indicato in precedenza, ogni associazione ha una direzione che definisce se si tratta di un'associazione di input o di output.</span><span class="sxs-lookup"><span data-stu-id="634bf-141">As we mentioned previously, each binding has a direction that defines whether it is an input or output binding.</span></span> <span data-ttu-id="634bf-142">I trigger sono sempre associazioni di input.</span><span class="sxs-lookup"><span data-stu-id="634bf-142">Triggers are always input bindings.</span></span> <span data-ttu-id="634bf-143">L'esempio precedente illustra una funzione attivata da un messaggio che viene aggiunto a una coda denominata **myqueue-items**.</span><span class="sxs-lookup"><span data-stu-id="634bf-143">The preceding sample shows a function that is triggered by a message being added to a queue named **myqueue-items**.</span></span> <span data-ttu-id="634bf-144">Il valore restituito della funzione viene quindi inviato alla tabella **outTable** nell'archiviazione tabelle di Azure.</span><span class="sxs-lookup"><span data-stu-id="634bf-144">It then sends the return value of the function to the **outTable** table in Azure Table storage.</span></span>

## <a name="creating-a-function-in-the-azure-portal"></a><span data-ttu-id="634bf-145">Creazione di una funzione nel portale di Azure</span><span class="sxs-lookup"><span data-stu-id="634bf-145">Creating a function in the Azure portal</span></span>

<span data-ttu-id="634bf-146">Azure offre modelli preconfigurati per gli scenari di funzioni di Azure più comuni.</span><span class="sxs-lookup"><span data-stu-id="634bf-146">Azure provides pre-configured templates for common Azure function scenarios.</span></span>

### <a name="quickstart-templates"></a><span data-ttu-id="634bf-147">Modelli di avvio rapido</span><span class="sxs-lookup"><span data-stu-id="634bf-147">Quickstart templates</span></span>

<span data-ttu-id="634bf-148">Per aggiungere una funzione di Azure, è necessario selezionare un'app per le funzioni.</span><span class="sxs-lookup"><span data-stu-id="634bf-148">To add an Azure function, you must select a function app.</span></span> <span data-ttu-id="634bf-149">L'app per le funzioni può essere identificata dalla saetta presente nell'icona con parentesi a punta.</span><span class="sxs-lookup"><span data-stu-id="634bf-149">The function app can be identified by the lightning bolt within pointy brackets icon.</span></span>  
![Screenshot che illustra un'app per le funzioni selezionata in un gruppo di risorse.](../images/5-function-icon.png)

<span data-ttu-id="634bf-151">Quando si aggiunge la prima funzione di Azure, viene visualizzata la schermata Avvio rapido.</span><span class="sxs-lookup"><span data-stu-id="634bf-151">When adding your first Azure function, you are presented with the Quickstart screen.</span></span> <span data-ttu-id="634bf-152">Questa schermata consente di scegliere un tipo di trigger e un linguaggio di programmazione.</span><span class="sxs-lookup"><span data-stu-id="634bf-152">This screen allows you to choose a trigger type and programming language.</span></span> <span data-ttu-id="634bf-153">In base alle selezioni effettuate, Azure genera quindi automaticamente il codice e la configurazione della funzione.</span><span class="sxs-lookup"><span data-stu-id="634bf-153">Then, based on your selections, Azure will generate the function code and configuration for you.</span></span> 
 
![Avvio rapido della funzione](../images/5-quickstart-form.png)

### <a name="function-templates"></a><span data-ttu-id="634bf-155">Modelli di funzione</span><span class="sxs-lookup"><span data-stu-id="634bf-155">Function templates</span></span>

<span data-ttu-id="634bf-156">La selezione dei modelli non è limitata a quelli visualizzati nell'Avvio rapido.</span><span class="sxs-lookup"><span data-stu-id="634bf-156">The selection of templates is not limited to the templates displayed in the Quickstart.</span></span> <span data-ttu-id="634bf-157">È possibile scegliere tra più di 30 modelli diversi.</span><span class="sxs-lookup"><span data-stu-id="634bf-157">You also have the option of choosing from over 30 different templates.</span></span> <span data-ttu-id="634bf-158">È possibile accedere alla schermata di elenco dei modelli durante la creazione delle funzioni successive o selezionando l'opzione **Funzione personalizzata** nella schermata Avvio rapido.</span><span class="sxs-lookup"><span data-stu-id="634bf-158">You can access the template list screen while creating subsequent functions, or by selecting the **Custom function** option on the Quickstart screen.</span></span>  
<span data-ttu-id="634bf-159">![Screenshot dell'interfaccia utente di avvio rapido di Funzioni di Azure che consente di essere subito operativi usando una funzione preconfezionata.](../images/5-template-list.png)</span><span class="sxs-lookup"><span data-stu-id="634bf-159">![Screenshot of Azure Functions Quickstart user interface to help you get started quickly with a premade function.](../images/5-template-list.png)</span></span>

## <a name="navigating-to-your-function-and-files"></a><span data-ttu-id="634bf-160">Passaggio alla funzione e ai file</span><span class="sxs-lookup"><span data-stu-id="634bf-160">Navigating to your function and files</span></span>

<span data-ttu-id="634bf-161">Quando si crea una funzione da un modello, vengono creati numerosi file.</span><span class="sxs-lookup"><span data-stu-id="634bf-161">When you create a function from a template, several files are created.</span></span> <span data-ttu-id="634bf-162">Ad esempio, se si è scelto di usare l'Avvio rapido per Webhook + API con JavaScript, i file generati saranno un file di configurazione, **function.json**, e un file del codice sorgente, **index.js**.</span><span class="sxs-lookup"><span data-stu-id="634bf-162">For example, if you opted to use the Webhook + API Quickstart using JavaScript, the files generated would be a configuration file, **function.json**, and a source code file, **index.js**.</span></span> <span data-ttu-id="634bf-163">Le funzioni create in un'app per le funzioni vengono visualizzate sotto la voce di menu Funzioni nel portale delle app per le funzioni.</span><span class="sxs-lookup"><span data-stu-id="634bf-163">The functions you create in a function app appear under the Functions menu item in the function app portal.</span></span>

<span data-ttu-id="634bf-164">Quando si seleziona una funzione nell'app per le funzioni, un editor di codice si apre e visualizza il codice relativo alla funzione, come illustrato nello screenshot seguente.</span><span class="sxs-lookup"><span data-stu-id="634bf-164">When you select a function in your function app, a code editor opens and displays the code for your function, as illustrated in the following screenshot.</span></span>

![Screenshot che illustra l'interfaccia di selezione del modello di funzione, in cui sono elencati i modelli che si possono usare per velocizzare lo sviluppo delle funzioni.](../images/5-file-navigation.png)

<span data-ttu-id="634bf-166">Come si può vedere nello screenshot precedente, a destra è visualizzato un menu a comparsa che include una scheda per la **visualizzazione dei file**.</span><span class="sxs-lookup"><span data-stu-id="634bf-166">As you can see in the preceding screenshot,  there's a flyout menu on the right that includes a tab to **View files**.</span></span> <span data-ttu-id="634bf-167">Selezionando la scheda, viene visualizzata la struttura di file che compone la funzione.</span><span class="sxs-lookup"><span data-stu-id="634bf-167">Selecting this tab shows the file structure that makes up your function.</span></span>  

## <a name="execution-options-for-testing-your-azure-function"></a><span data-ttu-id="634bf-168">Opzioni di esecuzione per testare la funzione di Azure</span><span class="sxs-lookup"><span data-stu-id="634bf-168">Execution options for testing your Azure function</span></span>

<span data-ttu-id="634bf-169">Dopo aver creato una funzione, è opportuno testarla.</span><span class="sxs-lookup"><span data-stu-id="634bf-169">Once you've created a function, you'll want to test it.</span></span> <span data-ttu-id="634bf-170">Esistono due approcci: l'esecuzione manuale e il test nel portale di Azure.</span><span class="sxs-lookup"><span data-stu-id="634bf-170">There are a couple of approaches: manual execution and testing from within the Azure portal itself.</span></span>

### <a name="manual-execution-of-a-function"></a><span data-ttu-id="634bf-171">Esecuzione manuale di una funzione</span><span class="sxs-lookup"><span data-stu-id="634bf-171">Manual execution of a function</span></span>

<span data-ttu-id="634bf-172">È possibile avviare una funzione attivando manualmente il trigger configurato.</span><span class="sxs-lookup"><span data-stu-id="634bf-172">You can start a function by manually triggering the configured trigger.</span></span> <span data-ttu-id="634bf-173">Se ad esempio si usa un oggetto HTTPTrigger, è possibile usare uno strumento come Postman o cURL per avviare una richiesta HTTP all'URL dell'endpoint della funzione.</span><span class="sxs-lookup"><span data-stu-id="634bf-173">For instance, if you are using an HttpTrigger - you can use a tool such as Postman or cURL to initiate an HTTP request to your function endpoint URL.</span></span>  

![<span data-ttu-id="634bf-174">Screenshot parziale dell'interfaccia dell'applicazione Postman con un URL di funzione digitato nel campo url e il corpo della richiesta compilato con un esempio di corpo.</span><span class="sxs-lookup"><span data-stu-id="634bf-174">Partial screenshot of the Postman application interface with a function URL typed into the url field and the request body filled out with a sample body.</span></span> ](../images/5-postman-execution.png)

<span data-ttu-id="634bf-175">Per ottenere l'URL dell'endpoint, selezionare Trigger HTTP dal riquadro di spostamento a sinistra e quindi selezionare il pulsante **Recupera URL della funzione**.</span><span class="sxs-lookup"><span data-stu-id="634bf-175">To get endpoint URL, select the HTTP Trigger from the left navigation, and then select the **Get function URL** button.</span></span>  

![Screenshot del portale delle app per le funzioni, con una funzione selezionata e l'azione *Recupera URL della funzione* selezionata nella parte superiore della pagina.](../images/5-get-function-url.png)

### <a name="testing-a-function-in-the-azure-portal"></a><span data-ttu-id="634bf-177">Esecuzione del test di una funzione nel portale di Azure</span><span class="sxs-lookup"><span data-stu-id="634bf-177">Testing a function in the Azure portal</span></span>

<span data-ttu-id="634bf-178">Il portale offre un altro modo pratico per testare le funzioni.</span><span class="sxs-lookup"><span data-stu-id="634bf-178">The portal also provides a convenient way to test your functions.</span></span> <span data-ttu-id="634bf-179">Sul lato destro della finestra del codice è presente un menu di spostamento con schede a comparsa.</span><span class="sxs-lookup"><span data-stu-id="634bf-179">On the right side of the code window, there is a flyout tabbed navigation menu.</span></span> <span data-ttu-id="634bf-180">In questo menu è presente la scheda **Test**.</span><span class="sxs-lookup"><span data-stu-id="634bf-180">This menu contains a **Test** item.</span></span> <span data-ttu-id="634bf-181">Se si espande il menu e si seleziona questa scheda, è possibile eseguire la funzione e visualizzarne il risultato.</span><span class="sxs-lookup"><span data-stu-id="634bf-181">Expanding the menu and selecting this tab gives you another way to execute your function and view the result.</span></span> <span data-ttu-id="634bf-182">In linea con lo scenario di HTTPTrigger, è possibile impostare il metodo HTTP e aggiungere i parametri di stringa di query e le intestazioni HTTP alla richiesta.</span><span class="sxs-lookup"><span data-stu-id="634bf-182">In keeping with the HttpTrigger scenario, you can set the HTTP method, and add query string parameters and HTTP headers to the request.</span></span> <span data-ttu-id="634bf-183">È anche possibile modificare il corpo della richiesta per testare altri scenari.</span><span class="sxs-lookup"><span data-stu-id="634bf-183">You may also change the request body to test additional scenarios.</span></span> <span data-ttu-id="634bf-184">Nell'esempio seguente il test è configurato come richiesta HTTP POST con un corpo della richiesta che ha un solo parametro denominato *name*.</span><span class="sxs-lookup"><span data-stu-id="634bf-184">In the following example, the test is configured as an HTTP POST request with a request body that has one parameter called *name*.</span></span>
 
![Screenshot della finestra Test del portale delle app per le funzioni che illustra una richiesta HTTP POST e un esempio di corpo della richiesta.](../images/5-portal-execution.png)

<span data-ttu-id="634bf-187">Quando si fa clic su **Esegui** in questa finestra di test, i risultati vengono visualizzati nella finestra di output insieme a un codice di stato.</span><span class="sxs-lookup"><span data-stu-id="634bf-187">When you click **Run** in this test window, the results are displayed in the output window, along with a status code.</span></span> 

## <a name="monitoring-an-azure-function"></a><span data-ttu-id="634bf-188">Monitoraggio di una funzione di Azure</span><span class="sxs-lookup"><span data-stu-id="634bf-188">Monitoring an Azure function</span></span>

<span data-ttu-id="634bf-189">La possibilità di registrare i messaggi e monitorare le funzioni è fondamentale durante lo sviluppo e nell'ambiente di produzione.</span><span class="sxs-lookup"><span data-stu-id="634bf-189">The ability to log messages and monitor your functions is critical during development and in production.</span></span> <span data-ttu-id="634bf-190">Il portale di Azure offre un dashboard di monitoraggio, nonché un modo per esaminare i log e le eccezioni di esecuzione derivanti dalle funzioni di Azure.</span><span class="sxs-lookup"><span data-stu-id="634bf-190">The Azure portal provides a monitoring dashboard as well as a way to review execution logs and exceptions obtained from your Azure functions.</span></span>

### <a name="log-window"></a><span data-ttu-id="634bf-191">Finestra registro</span><span class="sxs-lookup"><span data-stu-id="634bf-191">Log window</span></span>

<span data-ttu-id="634bf-192">È possibile aggiungere anche istruzioni di registrazione alla funzione.</span><span class="sxs-lookup"><span data-stu-id="634bf-192">You're also able to add log statements to your function.</span></span> <span data-ttu-id="634bf-193">Queste istruzioni vengono visualizzate nella finestra registro della funzione.</span><span class="sxs-lookup"><span data-stu-id="634bf-193">These statements will appear in the log window of the function.</span></span> <span data-ttu-id="634bf-194">La finestra registro si trova nel menu a schede a comparsa nella parte inferiore della finestra del codice.</span><span class="sxs-lookup"><span data-stu-id="634bf-194">The log window is located in a tabbed flyout menu located at the bottom of the code window.</span></span> <span data-ttu-id="634bf-195">Il seguente frammento di codice JavaScript illustra come registrare un messaggio usando il metodo `context.log`.</span><span class="sxs-lookup"><span data-stu-id="634bf-195">The following JavaScript code snippet shows how to log a message using the `context.log` method.</span></span>

```javascript
  context.log('Enter your logging statement here');
```  

![Screenshot della sezione dell'editor di codice dell'interfaccia utente di Funzioni di Azure.](../images/5-log-window.png)

### <a name="errors-and-warnings-window"></a><span data-ttu-id="634bf-198">Finestra degli errori e degli avvisi</span><span class="sxs-lookup"><span data-stu-id="634bf-198">Errors and warnings window</span></span>

<span data-ttu-id="634bf-199">La scheda della finestra degli errori e degli avvisi si trova nello stesso menu a comparsa della finestra registro.</span><span class="sxs-lookup"><span data-stu-id="634bf-199">You can locate the errors and warnings window tab in the same flyout menu as the log window.</span></span> <span data-ttu-id="634bf-200">In questa finestra vengono visualizzati gli errori e gli avvisi di compilazione all'interno del codice.</span><span class="sxs-lookup"><span data-stu-id="634bf-200">This window will show compilation errors and warnings within your code.</span></span> <span data-ttu-id="634bf-201">Poiché JavaScript è un linguaggio dinamico e interpretato, l'immagine seguente illustra un errore di compilazione in una funzione C#.</span><span class="sxs-lookup"><span data-stu-id="634bf-201">As JavaScript is a dynamic and interpreted language, the following image shows a compilation error in a C# function.</span></span>  

![Screenshot della sezione dell'editor di codice dell'interfaccia utente di Funzioni di Azure, con la finestra **Registri** evidenziata nella parte inferiore della schermata.](../images/5-errors-window.png)

### <a name="monitoring-dashboard"></a><span data-ttu-id="634bf-203">Dashboard di monitoraggio</span><span class="sxs-lookup"><span data-stu-id="634bf-203">Monitoring dashboard</span></span>

<span data-ttu-id="634bf-204">Nel menu di spostamento dell'app per le funzioni, quando si espande il nodo della funzione, viene visualizzata la voce di menu **Monitor** (Monitora).</span><span class="sxs-lookup"><span data-stu-id="634bf-204">In the function app navigation menu, once you expand the function node - you will see a **Monitor** menu item.</span></span> <span data-ttu-id="634bf-205">Il dashboard di monitoraggio offre un modo rapido per visualizzare la cronologia delle esecuzioni della funzione.</span><span class="sxs-lookup"><span data-stu-id="634bf-205">The monitor dashboard provides a quick way to view the history of function executions.</span></span> <span data-ttu-id="634bf-206">Questa visualizzazione mostra il timestamp, il codice di risultato, la durata e l'ID operazione, nonché un messaggio che indica se la funzione è stata completata o meno correttamente.</span><span class="sxs-lookup"><span data-stu-id="634bf-206">This view displays the timestamp, result code, duration, and operation ID, as well as if it completed successfully.</span></span> <span data-ttu-id="634bf-207">I dati vengono popolati usando Azure Application Insights.</span><span class="sxs-lookup"><span data-stu-id="634bf-207">The data is populated via Azure Application Insights.</span></span>  

![Screenshot del dashboard di monitoraggio avviato usando la voce di menu **Monitor** della funzione che visualizza un elenco di chiamate riuscite e non riuscite alla funzione.](../images/5-monitor-function.png)
